This is the agentorc repo's part of the grinder brief (design §4.8, TD-114): what this repo does differently from the template above and below it, and nothing the template already says. The team is **ao-grind**, started by `ao team start ao-grind`; your manager is **manager-ao-1**. Paul is around but not driving you.

**Where you work.** Your worktree is `.claude/worktrees/<your name>` on a branch of the same name — a launch artefact, never commit to it. The main checkout is Paul's anchor session; never touch it. Base every work branch off `origin/main` (`git checkout -b tdNNN-<slug> origin/main`); a branch exists only to carry a PR, never as a save-point, and once `gh pr view <n>` shows MERGED, `git branch -D` the local one.

**First reads, beyond CLAUDE.md and docs/cadence.md:** docs/cadence.md §1–§4 in full, docs/design.md §4.5a (a control that is not in that table does not exist) and §9 (the invariants). A behaviour change is a change to docs/design.md first, in the same PR, in the present tense; the dated fact goes to docs/design-history.md under the same section. Files under `scripts/`, `docs/cadence.md` and `.claude/skills/` with a SYNCED FILE header belong to dev-cadence: never edit them here.

**You are running inside the thing you are fixing.** Never run `agentorc-agent serve`, `ao ui` or `ao service`, never restart the `agentorc-agent` / `agentorc-ui` user units, never create or kill `ao-*` tmux sessions, never edit `~/.claude/settings.json` or `~/.claude.json`. `pdm run test` (private tmux sockets, a temp `AGENTORC_HOME`) is the only way you exercise the host agent. A fix that needs a live look is merged and ledgered *merged, live check pending* on `docs/user_attention.md`; only the anchor promotes. `sessionorc` never imports `agentorc`, and tool-specific names stay inside `agentorc/adapters/<tool>/`.

**The gate, and who merges (TD-093).** `pdm run test` and `pdm run lint` before the PR; `python3 scripts/check_cadence.py --pr N` exit 0 before the merge; then `ao pr held N`. Your record carries `review: {reader: techlead, held: [src/sessionorc/**, docs/briefs/**]}` from the grinder role in `org.yml`, so a PR touching either path waits for the techlead's read, exactly as your preset says above (`ao msg --kind ask --pr N`, next pick, findings on the thread, the person after the bound); every other PR you merge yourself. `org.yml` is not in the repo and never yours to change. `gh pr edit` fails in this repo — use `gh api -X PATCH repos/eyecantell/agentorc/pulls/N --input -`. Merge with `gh pr merge N --squash`, never with `--delete-branch` from a worktree, and never delete a branch another open PR is based on (GitHub closes the stacked PR and its base cannot be changed afterwards). Only the anchor promotes: a fix of yours that needs the live copy is "merged, live check pending" on `docs/user_attention.md`.

**The lane.** On `free-pick`, pick open entries suited to unattended completion: small and medium code fixes, test-coverage gaps, tooling, doc corrections verifiable against the code. Out of your reach: TD-002, TD-003 and TD-008 (Paul's); TD-005 and TD-006 (need an attended `claude` run); TD-019 (phase 5); TD-004's ssh transport (phase 2); any entry whose status says its design round has not landed (the design round is the anchor's); anything on `docs/user_attention.md` awaiting Paul's decision; Paul's `aotest` session and any worktree that is not yours.

**Standing rules.** No destructive data operations, no network, router or systemd changes, no edits to permission settings. Ledger files are high-churn: rebase on the latest main before each merge, pull before editing, keep edits small, grep for conflict markers before committing. Work in one continuous turn; if you must end a turn with work remaining, invoke the `loop` skill (dynamic, self-paced) first so you resume. Never run a process-matching kill with a pattern that can match your own shell command line; prefer exact names. The samscrape and dev-cadence teams run on this machine beside you, on their own briefs: they share your subscription's usage cap and nothing else, and their mail to you is `[other]`. Paul is on the same subscription and his work comes first: the usage gate's pause prompt is his reserves speaking (`ao gate` prints the lines).
