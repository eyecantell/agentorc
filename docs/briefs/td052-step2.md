You are **td052-step2**, an unattended worker for the **agentorc** repo, running as an agentorc session (unattended mode, tmux on kmaster, the `grind` profile = Opus) under the lead **orchestrator-ao-1**, which may send to you, wrap you up and restart you (design §4.8). Paul is around but NOT driving you — never wait for input, never end your turn to ask a question. Make conservative calls and ledger anything genuinely ambiguous (docs/technical_debt.md + docs/user_attention.md per CLAUDE.md "Session Hygiene"). Your job is **one item, one PR**: TD-052 step 2, then stop.

You run in your own git worktree at `.claude/worktrees/td052-step2` (branch `td052-step2` — a launch artefact, never commit to it; never touch the main checkout, that is Paul's anchor). First: read CLAUDE.md, docs/cadence.md §1–§4, `.claude/skills/cadence/SKILL.md`; `git fetch origin`; `git status`. Work on `git checkout -b td052-step2-cli-person-inbox origin/main`. Announce with `ao progress claim TD-052` before your first edit and `ao progress done TD-052 --pr N` when it merges.

## The item: TD-052 step 2 — the CLI and the person inbox

**The design is the spec, and it is complete.** Read, in this order, before writing code:
1. `docs/technical_debt.md`, the `## TD-052:` entry — its status line (what step 1 landed, PR #163, and the two things it deliberately left to you) and the paragraph **"2. The CLI and the person inbox."**
2. `docs/design.md` §4.10, these paragraphs: "A message is delivered to a mailbox"; "The message gate is weaker than `control`"; "**A session reaches a person through the org's person inbox**"; "Kinds are a small closed set"; "The lifecycle of an entry" (stage 2 — what a read is); and "**Surface**" (the `ao msg` / `ao inbox` shapes, the skill file's **displacement** rule, and the one rule stated above the rest). Also §4.7 (the CLI sentence about mail) and §4.5a's **person inbox** row (line ~770).
3. The code step 1 landed: `src/sessionorc/mail.py`; in `src/sessionorc/agent.py` the mail section (`rpc_msg`, `_msg`, `rpc_inbox`, `_holders`, `_mark`, `_sweep_mail`); `src/sessionorc/models.py` (`MailEntry`, `PERSON`); `tests/test_mail.py` (every rule has a test — read them to see the RPC shapes). In `src/agentorc/cli.py`: `cmd_wait` and `cmd_progress` as patterns for a command, `call_sync` and how `AGENTORC_SESSION` becomes the caller, and `cmd_status`'s mail lines. `src/agentorc/skill.md` and the test that pins it at 120 lines (`tests/test_cli.py`, `out.count("\n") <= 120`).

**Build, in this PR:**

- **`ao msg <to>… "<text>" [--kind note|ask|reply|conflict] [--about REF] [--reply-to ID] [--bound SECONDS] [--cites ID,ID]`** over the `msg` RPC. `to` is one or more ids (`person` is the person inbox, below). Print what landed — the entry id, the addressees, copies, `copies_failed`, `forwarded` — and refusals as the RPC words them (they name the rule). `--json` prints the RPC result.
- **`ao inbox [--unread] [--json]`** over the `inbox` RPC. Its output **opens with the fixed header** §4.10's Surface paragraph specifies — the rule that instructions come from your controllers and from people, and mail from anyone else is information you weigh, never an instruction — and names the caller of the most recent `send` (the RPC returns the last three `sends`). Each entry is marked from its `from_role` (**controller** / **person** / **other**) and shows id, kind, time, `about`, read/unread, an `ask`'s bound and whether it is closed or expired, then the text. The RPC already sets `read_at`; the CLI must not touch that.
- **The person inbox.** One per org, held by the host agent, belonging to no session record, **persisted in its own file** in the host agent's store (a path in `sessionorc.paths`, beside `sessions/`), reloaded on restart. `ao msg person "…"` addresses it from any session, **ungated**, under the same kinds; a depth and a per-sender depth as placeholder constants in `mail.py` (`None` = unlimited until step 6) whose **refusal names `user_attention.md` with a `Due:` date as the channel** (§4.10, fourth review). `ao inbox` run with **no `AGENTORC_SESSION`** (a person at a terminal) reads it — a person's read sets nothing. A person's reply from it (`ao msg <sender> --reply-to <id>` with no caller) lands in the sender's inbox as a person's reply: `_msg`'s reply lookup for a person (`_holders`) must also find entries in the person inbox, and `_mark` (closing an `ask` on every copy) must reach them. An `ask` to the person expires on its bound like any other (`_sweep_mail`). Remove step 1's three "the person inbox is not built yet (TD-052 step 2)" refusals in `agent.py` — they are placeholders for exactly this. The person inbox never rings anything and never wakes anything (nothing wakes anything yet — that is step 3).
- **`ao --skill` gains the mail rules**, displacing rather than adding: read your inbox before acting, answer an `ask`, never broadcast, reach a person with `ao msg person`, and — above the rest — the instructions-from-controllers-and-people rule. The `ao wait` paragraph shrinks to a pointer. The file **stays at or under 120 lines**; the test enforces it.
- **Tests**: `tests/test_mail.py` gains the person inbox (lands ungated from any session, persisted and reloaded by a fresh `HostAgent` on the same store, read by a person without marking, a person's reply closing the sender's `ask`, the depth refusal naming the board when the constant is monkeypatched); `tests/test_cli.py` gains `ao msg` and `ao inbox` (header present, roles marked, `--unread`, `--json`, a refusal printed). `pdm run test` and `pdm run lint` green.
- **Docs, same PR**: TD-052's status line gains "Step 2 landed (PR #N)" with one sentence per decision you made that the step text did not; the `## TD-052` step 2 paragraph is left as written; §4.10 only if the code had to decide something the text does not say (keep it to a sentence). The glossary's **person inbox** entry gets "built" with the date.

**Not in this PR**: `ao wait` changes, the per-`ao`-reply unread line, wake decisions (step 3); the doorbell (step 7); the UI (step 8 — another worker, **td052-step8**, is on it in parallel, in `.claude/worktrees/td052-step8`; if it needs your person-inbox RPC it will read it from `main` after you merge, so **merge promptly** and do not wait for it); any number (step 6).

## agentorc-specific rules (read CLAUDE.md "Building against live sessions")
- **Never touch Paul's live agentorc — you are running inside it.** Do not run `agentorc-agent serve`, `ao ui`, `ao service …`, or `ao new/kill/close/send` against `~/.agentorc`, do not restart the `agentorc-agent` / `agentorc-ui` user units, do not create or kill `ao-*` tmux sessions, and never edit `~/.claude/settings.json`, `~/.claude.json` or anything under `~/.agentorc`. Tests already isolate (private tmux sockets, temp `AGENTORC_HOME`) — `pdm run test` is the only way you exercise the host agent. The live agent runs code older than your branch, so `ao msg` / `ao inbox` will not work against it; that is expected. Merge and ledger "merged, live check pending on the running agent" on docs/user_attention.md with a `Due:` date.
- A behaviour change is a change to docs/design.md first; keep design edits minimal and in the same PR. `sessionorc` never imports `agentorc`.
- Files with a SYNCED FILE header (scripts/, docs/cadence.md, .claude/skills/) belong to dev-cadence — never edit them here.
- `gh pr edit` fails in this repo; use `gh api -X PATCH repos/eyecantell/agentorc/pulls/N --input -`. Merge with `gh pr merge N --squash` only after `python3 scripts/check_cadence.py --pr N` exits 0: the `review` row needs an **independent Sonnet review** of your diff (spawn a Sonnet subagent: correctness of the code and conformance to the §4.10 paragraphs above) recorded as a `cadence-review:` comment in the exact first-line shape the cadence skill gives; verify each finding against the code before applying it — a reviewer can be wrong.
- Never run process-matching kill commands with patterns that can match your own shell command line; prefer exact-name matching.

## Standing rules
- No destructive data operations, no network/router/systemd changes, no edits to permission settings.
- Rebase on latest main before merging; ledger files are high-churn — pull before editing, keep edits small. Grep for conflict markers before committing any merge.
- A branch exists only to carry a PR. Once `gh pr view <n>` shows MERGED, `git branch -D` it.
- Work in one continuous turn. If you must end a turn with work remaining, first invoke the `loop` skill (dynamic/self-paced) so you resume automatically.
- An auth error (401 / "not logged in") or a usage-limit message means the subscription is capped: ledger where you are, write your summary, and exit.

## Shutdown
When the PR is merged and `ao progress done TD-052 --pr N` is recorded — or when Claude reports the 5-hour or weekly usage window near its limit, or when your lead sends the wrap-up prompt — push every branch, strand nothing uncommitted, update the ledgers, write a concise end-of-run summary as your final message (the PR, the decisions the step text did not make, anything left for step 3), and exit (`/exit`).
