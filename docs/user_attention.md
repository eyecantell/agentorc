# User Attention Board

Items that need the user to act or decide, plus in-flight work parked by a session. Sessions add entries **the moment they arise** and remove them when handled. Keep this file small — durable debt belongs in [technical_debt.md](technical_debt.md); this board is only "a human must act or decide."

All conventions — entry format and the `<host>` semantics, `Due:` dates and snoozing, the `Swept:`/`Swept-deep:` sweep stamps — live in [docs/cadence.md §3](cadence.md#3-never-strand-work). That file is SYNC (improvements reach every consumer on re-sync); this skeleton is SEED (yours, never overwritten), which is why the explanations do not live here (dev-cadence TD-17).

Format: `- [ ] YYYY-MM-DD (session <first-8-of-session-uuid> on <host>, or n/a) — what's needed. Context: TD-NNN / PR #N / branch. Due: YYYY-MM-DD.`

## Needs the user

- [x] 2026-09-23 (session `manager-ao-1` on kmaster) — **ao-grind wound down at 02:50Z 2026-09-24 UTC. Done 2026-09-23 (the anchor): nothing waited on Paul — #506 (TD-121) and #507 (TD-123) were merged by the manager and the anchor promoted main at 9e41e46 to the live venv; the report itself is the shape TD-125 replaces with an FYI note (merged #508).** Was: 2026-09-24 (session `manager-ao-1` on kmaster) — **ao-grind ran out of work at 02:50Z and is wound down. Nothing waits on your read this run — neither PR touched `src/sessionorc/`, `docs/briefs/` or `org.yml`.** The run started at 02:18Z on TD-121 and TD-123. `ao team stop ao-grind --close` closed both grinders clean: nothing unpushed, no outcome owed. What each looked for and did not find, from the `why` on its record: **grinder-ao-1** re-read the ledger after #506 merged — 0 pickable (TD-121 done by it as #506, TD-123 done by grinder-ao-2 as #507; the other 58 open entries are live-check, evaluation, design-first, decision, or build entries marked Paul's/phase-2/attended/synced/anchor-or-briefs/open-PR). **grinder-ao-2** found the same after #507 merged: TD-121 was grinder-ao-1's live lease at the time, and the remaining 58 entries are excluded by the same header reasons. Merged this run, both cadence-checked pass (review row self-attested, recorded not verified): #506 (TD-121, more-menu placement) and #507 (TD-123, dead top-bar tabs). One note worth flagging: grinder-ao-1 first reported #506 "merged" while it was still open and conflicting with #507's ledger append; it caught this itself within 5 minutes, rebased and actually merged — no action needed, but its self-report was momentarily wrong. The techlead seat was filled once (asks: 0, answered: none, passed up: none) and closed itself empty — host agent's own tick, no manager action. To start again: `ao team start ao-grind`. Context: TD-121 / TD-123 / PR #506 / #507 / docs/briefs/manager-ao-1.log. Due: 2026-09-24.
- [x] 2026-09-23 (session `manager-ao-1` on kmaster) — **Done 2026-09-23 (the anchor): #498 read, rebased twice past the moving ledger, merged and promoted (main `90b0492`); the team was restarted at 02:18Z 2026-09-24 on TD-121 and TD-123.** Was: **ao-grind ran out of work at 23:47Z and is wound down. One `src/sessionorc` PR still waits on your read.** The run started at 23:08Z. `ao team stop ao-grind --close` closed both grinders clean: nothing unpushed, no outcome owed. **Waiting on you (TD-093 hand rule):** #498 (TD-119, "no self-decide", opened 23:20Z) — reviewed SHIP, touches `src/sessionorc/`; under two hours old at wind-down, so no separate ask, just this line. What each grinder looked for and did not find, from the `why` on its record: **grinder-ao-1** re-read the ledger after #499. TD-119 is the only other pickable entry and rides in #498 (its own PR); everything else is anchor/paul/dev-cadence owned, design-first or a live check. **grinder-ao-2** found the same: TD-119 already claimed by grinder-ao-1 and in its lane; the rest is 55 excluded entries (anchor evaluation/design-first/build/live-check/decision, paul's, dev-cadence's). Merged this run, all cadence-checked pass: #499 (TD-071 items 1-2), #500 (TD-046). The techlead seat was filled at start and closed itself empty (host agent's own tick, no manager action). To start again: merge #498, then `ao team start ao-grind`. Context: TD-093 / TD-119 / TD-071 / TD-046 / PR #498 / #499 / #500 / docs/briefs/manager-ao-1.log. Due: 2026-09-24.
- [ ] 2026-09-23 (session `grinder-ao-2` on kmaster) — **Merged, live look pending: Pop out (TD-046).** After the next promote: *more ▾ → Pop out* on two working cards opens two windows with no top bar; alt-tab moves between them and each title reads `<name> · <state>` (`▲ ` in front while it needs you); Pop out again raises the first window instead of opening another; the card's link reads *Focus window* while it is open. Context: TD-046 (archived), PR #500. Due: 2026-09-26.
- [ ] 2026-09-23 (session `grinder-ao-1` on kmaster) — **Merged, live look pending: Deny with a reason (TD-117).** After the next promote, on your browser walk: a pending permission's Deny on the card, the Focus header and the Inbox row has a *why? (optional)* box beside it; what you type goes to the session with the refusal, and an empty box is a bare Deny. Context: TD-117 (archived), PR #486. Due: 2026-09-26.
- [ ] 2026-09-23 (the anchor session on kmaster) — **On hold (Paul, 2026-09-23): the guardians team setup and the contractmatch container node.** Nothing is built toward either until Paul lifts the hold: `docs/briefs/guardians-orchestrator.md` stays unlaunchable (the repos are not on this host; the devcontainer question in design §10 is open), and TD-057's one-time node steps (hosts.yml `nodes:`, the contractmatch postCreate re-point, `~/.agentorc/nodes/contractmatch/env`, `ao host up`, the cross-link checks of PRs #211–#224) stay unrun — their text is in this file's history at the two lines folded here. When lifted, the node first (it is the guardians' substrate), then the brief. Context: TD-057 / TD-055 step 4 / design §4.4a, §10. Due: 2026-10-31.
- [ ] 2026-09-23 (the anchor session on kmaster) — **One browser walk, Paul's eyes only — everything the board asked you to look at, in page order.** The anchor has seen the DOM half of each headless; what is left is how it reads. **Org:** every card the same height with its six rows; a team header `host / repo · 2 working · 1 idle`; an ended techlead seat as *◇ on call* with **Message…** first; *mine* beside the filter; the usage chip *grind · week 89%* (TD-095, TD-097). **Focus:** JetBrains Mono under WebGL — `=>` as two characters, box borders unbroken (TD-038); on an unattended worker a grey read-only line, no composer, **Take over** in the header, then **Hand back** (TD-096); press **Close** and see one *pane is gone* line and no reconnect loop in the UI journal (TD-029); the composer reading **→ Steer** on a working session and disabled on a killed one (TD-047); the **stops** badge editing the stop time (TD-026); the **Reports** panel with Drop and the grants chip (TD-028). **Inbox:** one centred column, sections as headings, the fourth section *Waiting on them*, FYI opening itself on something new, **Dismiss all** confirming with the right count, Snooze on `stalled?` and unpushed rows, a trail row saying how a state resolved (TD-079, TD-073, TD-082, TD-085); ✉ Message from a card's `more ▾` and Reply in the Focus Inbox panel (TD-052 step 8). Say what is off and the anchor ledgers it. Context: TD-095 / TD-097 / TD-038 / TD-096 / TD-029 / TD-047 / TD-026 / TD-028 / TD-079 / TD-073 / TD-082 / TD-085 / TD-052. Due: 2026-09-30.
- [ ] 2026-09-23 (the anchor session on kmaster) — **Four findings to file as dev-cadence TDs (Paul, 2026-09-23) — no dev-cadence session was up to take them, so this line holds them until one is.** (1) A `review` row that failed on a merged PR can never clear (`row_review` compares the comment's `created_at` to the merge time), so a manager's *second fail on the same PR* rule punishes a worker that answered honestly — an *acknowledged* state the sweep can skip, or sweeps reporting unclearable fails apart from actionable ones (agentorc PR #128, 2026-09-14). (2) The `deploy` row answers from the invoker's clone (`row_deploy(pr, root)`), so a behind checkout turns `fail` into `na` (samscrape #791–#796, 2026-09-12) — read at `origin/<default>` as `nudge_user_attention.py --fetch` does, or say `unknown` when behind. (3) The `review` row names the *last* `cadence-review:` comment, so an author's later disclosure comment reads as a self-review (samscrape #830/#833/#838/#839) — name the first match. (4) `cadence_hooks.sh --session-start` runs `nudge_user_attention.py --report --due-only --fetch` over every repo in the roster — 71 items, about 4,200 tokens, at every unattended start, restart and seat fill (TD-118): scope it to the session's own board, without `--fetch`, when the session is unattended (`AGENTORC_SESSION` set and the launch layer says unattended), and keep the machine-wide view for `/attention`. The evidence in full is in this file's history at the three 2026-09-12/14 lines folded here. Context: dev-cadence `scripts/check_cadence.py`, `scripts/cadence_hooks.sh`, cadence §4, §5, §7; TD-118. Due: 2026-09-30.
- [x] 2026-09-23 (session `manager-ao-1` on kmaster) — **Done 2026-09-23 (the anchor): every PR named here read, merged and promoted (main `687d6f6` that morning, `b5287c0` in the afternoon).** Was: **ao-grind ran out of work at 07:18Z (01:18 MDT) and is wound down. Three `src/sessionorc` PRs wait on your read, and two more are stacked behind them.** The run started at 05:35Z. `ao team stop ao-grind --close` closed both grinders clean: nothing unpushed, no outcome owed. **Waiting on you (TD-093 hand rule):** #470 (TD-115, exit-hook grace, opened 05:42Z), #474 (TD-069 step 3, the board write-back RPC, 06:17Z) and #477 (TD-093 slice 1, the review field, 06:46Z). Each is reviewed. Behind them: #472 (TD-069's Inbox board rows) waits on #474, and draft #479 (TD-093 slice 3, the UI) is parked on #477. What each grinder looked for and did not find, from the `why` on its record: **grinder-ao-1** re-read the ledger after #481. Every open entry is yours or needs a live check, is attended-only or phase 2, is the anchor's design or paths, lives in dev-cadence's synced files, is unapproved or for evaluation, or is an unreproduced flake. TD-062's service upgrade needs a design call, because its refuse-while-a-team-is-live rule looks stale. **grinder-ao-2** found its lane empty once #472 went to you. The rest of the ledger is grinder-ao-1's files, design-first, for evaluation, synced, `src/sessionorc`-first or yours. Merged this run: #469, #471, #473, #475, #476, #478, #480 and #481, all passing the cadence check (the review row is self-attested, so it is recorded, not verified). Every other fail in the last 12 hours is the anchor's own: #454 and #430 on ci, #441 on worktree. The techlead seat was filled once by the host agent (05:44Z, for 1 ask) and was closed empty. **Also seen once:** at 06:58:45Z grinder-ao-1's record read `needs-you`, and its pending question was Claude Code's suggestion-mode text (*"Suggestion mode asks me to predict what you'd type next …"*). Seconds later it was back to `working` by hook, so no ledger holds it yet. To start again: merge #470, #474 and #477 in any order, then #472, then `ao team start ao-grind`. Context: TD-093 / TD-115 / TD-069 / PR #470 / #474 / #477 / #472 / #479 / docs/briefs/manager-ao-1.log. Due: 2026-09-23.
- [x] 2026-09-22 (session `grinder-ao-1` on kmaster) — **Done 2026-09-23 (the anchor): live check passed — the two seat exits after the promote (06:17Z, 13:57Z) raised no alarm; both old alarms dismissed; TD-115 archived.** Was: **Live check pending once #470 is merged and promoted: the identity alarms a session's own `/exit` raised (TD-115).** The two *outside claimed ao-agentorc-techlead-ao-1 on hook* alarms (2026-09-23 03:33Z and 03:55Z) came from a hook that arrived just after its pane had closed; they are no longer raised. After the promote, **Dismiss** both on the Inbox. The next few techlead seats the tick closes should raise none, and none should read `exited (hook)` after a refusal. Context: TD-115 / PR #470. Due: 2026-09-26.
- [x] 2026-09-22 (the anchor session on kmaster) — **Decided by Paul 2026-09-23: the repo is renamed together with TD-060 step 2, at a release boundary with no team running.** As written: **Decide when the GitHub repo `eyecantell/agentorc` is renamed to `shiftlead`** — Paul decided the name 2026-09-22 (*go with the rename to shiftlead, we will have to figure out when to rename the repo*). Two orders work: rename the repo first (GitHub redirects the old URL, so `gh` and every remote keep working until they are re-pointed) or together with step 2, the machine-name cut at a release boundary with no team running. Either way dev-cadence's synced files and samscrape's `.agentorc.yml` follow. Context: TD-060. Due: 2026-09-29.
- [x] 2026-09-22 (session `manager-ao-1` on kmaster) — **Handled 2026-09-22 (the anchor, on Paul's word *merge 093*): #406 read and merged as 7368415, with one fix — grinder-ao-1's brief had no hand rule at all (why #402 slipped); both grinder briefs now carry it.** As written: **Read and merge PR #406: your hand rule (a member's PR touching `src/sessionorc/`, `docs/briefs/` or `org.yml` waits for you; an ask after two hours) written into the manager brief.** You asked for it in your note of 2026-09-22 after #402 merged without your read. The manager left #406 open without review, because it touches `docs/briefs/` itself. Until it merges, the next `ao team start ao-grind` runs a manager that does not watch for this. Context: TD-093 / PR #406 / PR #402. Due: 2026-09-23.
- [x] 2026-09-22 (the anchor session on kmaster) — **Decided 2026-09-22 (Paul: *agreed on 30,10*): the standing reserves are 30% per session and 10% per day of the weekly window; the anchor writes them into the two briefs after tonight's reset (17:59 MDT), in place of the 95%.** As written: **The weekly wind-down figure in the briefs is 95%, on your word this morning, and you called it temporary.** `docs/briefs/manager-ao-1.md` and `grinder-ao-1.md` now say *near its limit* is 95% of the weekly window (the team stopped itself at 89% on 2026-09-21). **Paul, later that morning: it drops back after this week's reset (2026-09-22 17:59 MDT), and the figure should follow his interactive load week by week — TD-100.** Say the figure to write back once the window resets; the anchor edits the two briefs and the team picks it up at its next start. Context: TD-100 / TD-099. Due: 2026-09-23.
- [x] 2026-09-21 (session `grinder-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending: `ao status -v` now says which commit the host agent was built from (TD-062 (c), PR #402).** At the next promote, `pip install --upgrade` builds the first wheel that carries a build record. After that, `ao status -v` (and `ao service status`) should print a line like `host agent: built from <sha> at <time>, started <time> — current with origin/main`. Once a later PR merges without a promote, the line should instead say *origin/main is N commits ahead of it, not live until the next promote*. Until that first promote it reads *build unknown*, which is expected. Due: 2026-09-24.
- [x] 2026-09-21 (session `grinder-ao-2` on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **Merged, live check pending: Focus watches (TD-096).** After the next promote, open an unattended worker's Focus page. You should see: a grey line in the terminal saying it is read-only, no composer, and **Take over** in the header; typing or pasting only toasts *watching*, and the mouse wheel still scrolls its history. Press **Take over**: the button becomes **Hand back**, the composer opens, the terminal re-attaches and takes keys, and its manager leaves it alone. **Hand back** returns it — and clears a stop time that passed meanwhile. That use is TD-096's *done when*. Also left for the anchor: `docs/briefs/manager-ao-1.md` should say, as the preset now does, that an `interactive` member is left alone. Context: TD-096. Due: 2026-09-23.
- [x] 2026-09-21 (the anchor session on kmaster) — **Decided 2026-09-21 by Paul: "techlead merges" is a per-role setting (`review:` on a role preset), every PR by default, approval required for an unattended session's PR and a recommendation for an interactive one's; the design is updated (TD-093) and *seat* is in the glossary. Nothing more is needed from you until the build.** As written: look at the shape of TD-093 (design §4.9b *The reader*, PR #397). Context: TD-093. Due: 2026-09-24.
- [x] 2026-09-21 (session `grinder-ao-2` on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **Merged, live check pending: the Org page's new card (TD-095, all six slices) — its *done when* is your look at the live page.** **Live since 2026-09-21 18:29 MDT** — the anchor merged #383 and #385 and promoted; the anchor's own headless look found row 2 and row 4 truncating on a narrow card (TD-095 notes it). What you should see on the Org page: every card the same height, six rows — name and pill; role, the mode as a word (*interactive* with a person mark, bold), the one clock; `branch <name>`; tool · account · model with the report at the right; a two-line slot with a caption (*ready to close ✓*, *says · 2m ago*, *via hook · 4m left*); a quiet foot whose first button is the next act (only Allow is filled). *working* is green, idle blue, *idle · unseen* blue with ●, exited / closed / unreachable grey. A team's header says `host / repo · 2 working · 1 idle` and no longer repeats its manager; *No team* says its count. **mine** beside the filter shows only your own sessions. Optional: `~/.agentorc/ui.yml` with `open_in: none`, or `{label: Zed, url: 'zed://ssh/{remote}{path}'}`, changes the editor button (no `cursor` preset — its docs do not document the link; §5 has the template). **Added 2026-09-22 (grinder-ao-2), live at the next promote:** a team's techlead seat that has ended reads *◇ on call* in grey, not *exited*, with *on call — comes on the next question*, *last came · 2h ago*, **Message…** as its first button, and the header counts *1 on call* (TD-097, PR #412 — its *done when* is this look too); *idle · unseen* now shows only on your own interactive sessions, a finished worker is plain *idle*; the team header's title is as large as a card's name (TD-095 (f), (i), PR #414); the top bar's usage chip reads *grind · week 89%* (was *grind wk 89%*) and *grind: no reading yet*, and the techlead's badge *Tech Lead* (TD-095 (g), (h), PR #421). Tell the anchor what is off; the design choices are §4.5 *The card's anatomy*. Context: TD-095, TD-097 / PRs #381 #384 #386 #388 #389 #390 #391 #412 #414 #421. Due: 2026-09-23.
- [x] 2026-09-21 (session `manager-ao-1` on kmaster; rewritten for the evening run) — **Handled 2026-09-21 18:28 MDT: #385 merged by the anchor and promoted a minute later; nothing of the run is left waiting.** As written: **ao-grind ran out of work again at 00:16Z (18:16 MDT) and is wound down; nothing of it is left but one `src/sessionorc` PR that waits on the anchor's merge (TD-093).** #385 (TD-095, gitinfo reads the commit for *detached at `<sha>`*; opened 23:22Z, two hours at 01:22Z), reviewed; the TD-095 line above names it too. #383 (TD-095's report line), the other one, was merged at 00:18Z. The run started at 22:56Z as the techlead trial (TD-075 step 5). grinder-ao-1 built TD-095's mockups and legend (#380, #387) and declared out of work at 23:33Z; grinder-ao-2 built TD-095's page (#381, #384, #386, #388–#391) plus its live-check board line (#392) and TD-071's struck items (#393) and declared out of work at 00:16Z; the why on each record lists what the rest of the ledger waits on (Paul, the anchor, a design, a live check). `ao team stop ao-grind --close` closed both clean, with nothing unpushed and no outcome owed. Every PR merged in the last 12 h (42) passes the cadence check; the review row is self-attested, so it is recorded, not verified. The techlead seat was filled twice (23:24Z for 3 steers, 23:47Z for 1), idle again within a minute each time, and passed nothing up; the numbers are in the manager's log for TD-075. The earlier run's #371 and #373 were merged at 18:53Z. To start again: merge #385, then `ao team start ao-grind`. Context: TD-095 / TD-075 / TD-093 / PR #385 / docs/briefs/manager-ao-1.log. Due: 2026-09-22.
- [x] 2026-09-21 (session `manager-ao-1` on kmaster) — **Done 2026-09-21: the anchor merged all four, 17:49Z–17:55Z.** As written: **Four of grinder-ao-1's PRs wait on the anchor's merge (TD-093: three touch `src/sessionorc`, #362 a brief); the oldest has waited over two hours** (the anchor asked for this line and an ask at two hours, both sent 17:15Z). #361 (TD-075 step 4, the thread after `TECHLEAD_WAIT`, opened 15:14Z), #362 (the manager's seat rule, 15:34Z), #363 (the RPC replies view, 15:44Z), #364 (copies follow resume, 15:58Z): each has a `cadence-review:` comment (FIXED/SHIP/SHIP/SHIP, Sonnet), and CI on #361 is green. grinder-ao-1 is not blocked yet: it asked for a restart at 17:05Z (long context) and was restarted at 17:06Z, and its new run rebases each PR as the one before it lands. Merge them in order. Context: TD-075 / TD-093 / PR #361 / #362 / #363 / #364. Due: 2026-09-21.
- [x] 2026-09-21 (session `manager-ao-1` on kmaster) — **Done 2026-09-21 (the anchor): all five read and merged in order — #346, #347, #348, #349, #353 — the conflicts between them resolved by the anchor since their author had wound down; and Paul decided the rule stays (the anchor reads every `src/sessionorc` PR before it merges) and asked for it as configuration, which is TD-093.** As written: Five of grinder-ao-1's `src/sessionorc` PRs wait on the anchor's merge, and they are why it ran out of work (updated 2026-09-21 01:31 at the team's wind-down). #346 (TD-075 step 2, `--source` + answered-for-you FYI), #347 (step 3, `--pass-up`), #348 (step 4 part 1, `asks_waiting` + `ao inbox --sent`), #349 (step 4, `ao new --keep-mail`): each green on CI with a `cadence-review:` comment (FIXED/FIXED/FIXED/SHIP, Sonnet), opened 05:40Z–06:44Z; and #353 (TD-076's `src/sessionorc` half of the *Waking a manager* comment sweep), green, `cadence-review: SHIP` (Haiku, docs), opened 07:22Z. The manager's seat rule needs #348 and #349, the retention needs #346, the passed-up exclusion needs #347 (TD-075 *Where it stands*, #351). grinder-ao-1 declared `restart` at 07:18Z with that as its reason and was restarted at 07:18:51Z; its fresh run found nothing else it may pick and declared out of work at 07:27Z, naming these five as its own blocked work. Merge in order (stacked steps; a later one may need a rebase after an earlier lands), and let each one's `cadence-review:` comment exist before the merge — #343 was merged 38 s before its comment and fails the `review` row for good. Context: TD-075 / TD-076 / PR #346 / #347 / #348 / #349 / #353. Due: 2026-09-21.
- [x] 2026-09-21 (session `manager-ao-1` on kmaster) — **Done 2026-09-21: the five were merged and the team started again at 14:22Z.** As written: **ao-grind ran out of work at 07:27Z (01:27 MDT) and is wound down** (`ao team stop ao-grind --close`: both grinders closed clean, nothing unpushed, no outcome owed). What each looked for and did not find, from the `why` on its record: **grinder-ao-1** (third run) re-read the ledger at origin/main 6deb255 — its own remaining work (TD-075 step 4, TD-076's sessionorc sweep) waits on the five PRs in the line above; the rest is sessionorc work the anchor merges (TD-057, TD-062), design-first or unapproved (TD-035/039/046/069/071/072/088/091/092), yours (TD-002/003/008/060/064), attended-only (TD-005/006), watching or live checks (TD-038/058/063/078), dev-cadence's (TD-050/061), or siblings' (TD-052/077; TD-079 and TD-081, also named on its record, are archived). **grinder-ao-2** (second run, restarted on your instruction for the TD-076 heading sweep, merged as #344): yours, attended-only, host-agent or node work, live browser checks already on this board, design-first entries, TD-080 (1) and TD-076 (4b)/(5) the anchor's, dev-cadence synced files. 14 PRs merged by the team this run (#333–#340, #343–#345, #350, #351, #354), all cadence-clean but #343 (`review`: merged 38 s before its comment). To start it again: merge the five, then `ao team start ao-grind`. Context: TD-053 / TD-075 / TD-076 / PR #352. Due: 2026-09-21.
- [x] 2026-09-20 (session `grinder-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending: the doorbell (TD-052 step 7, PR #343). After the next promote, the host agent types into idle unattended workers when mail arrives for them.** What you should see: send a worker mail while it sits idle (`ao msg <worker> "…"` from its manager, or Message on its card). Within a few seconds its pane shows `[agentorc] you have N unread messages — run ao inbox` submitted as a prompt, and the worker runs `ao inbox`. Nothing the sender wrote should appear in the pane. It should *not* ring: your own interactive sessions, a worker with half-typed text in its composer, or a worker that was just sent Wrap up or `ao team stop`. If a ring won't submit, `ao status -v` shows `doorbell failed …` under that session. If rings misbehave, the quick fix is to revert PR #343 and promote again. Context: TD-052 / PR #343. Due: 2026-09-23.
- [x] 2026-09-20 (session `grinder-ao-1` on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **Merged, live check pending: the Focus terminal now uses a bundled JetBrains Mono and draws through WebGL (TD-038, PR #336).** After the next promote, open any session's Focus page in your normal browser. You should see: JetBrains Mono in the pane even on a machine without it installed; `=>` typed or printed as two characters, not an arrow; and Claude Code's box-drawing borders unbroken. Headless Firefox has no WebGL, so only the DOM fallback was seen. If the pane is ever blank or garbled, say so, and the addon is the first suspect (removing one `<script>` line in `focus.html` reverts it). Context: TD-038 / PR #336. Due: 2026-09-24.
- [x] 2026-09-20 (session `tdgrind-ao-1` on kmaster) — **Handled 2026-09-21 00:27: the anchor merged and promoted all three (#297, #301, #303), and the lead's cadence check passes every row on each. The person inbox is still worth a pass.** Three of my PRs were green, reviewed and waiting on the anchor, and my person-inbox slot is full so this is the channel that reaches you.** All three are `src/sessionorc`, which your 17:0Z instruction reserved. **#297** (TD-088): the two trail tests stop the tick loop instead of racing it, after the review showed my first fix only narrowed the window; it also files TD-088's open half — a row that ends because its session *exited* is trailed as *resolved*, which §4.10 defines as *the home cannot tell*, so a person whose question a worker died holding reads it as if it sorted itself out. That word is a design call and is yours. **#301** (TD-077 a2, Suspend): two rounds of findings, both real and both closed — Forget was an open road (the acting gate lets a controller forget an unattended member, freeing the name), and a suspend asked at a *node* was served there, killing the session and writing a home-owned mark the home's next copy wipes. **#303** (TD-086 item 1): a lead's `ao wait` now rides out a promote instead of dying with it. Nothing of mine is unpushed; every branch is rebased on main and every one carries its `cadence-review:` comment. **Also for you, not a PR:** the person inbox refused my last note as full (20 unread from me) — worth a pass through it, since that is where every outcome I owe you is going to land. Context: PRs #297, #301, #303 / TD-088, TD-077, TD-086. Due: 2026-09-21.
- [x] 2026-09-21 (session `tdgrind-ao-1` on kmaster) — **Handled 2026-09-23 (the anchor): all four merged and promoted on 2026-09-21.** As written: **Four PRs of mine are green, reviewed and waiting on the anchor, and they finish the TD-077 lane you handed me.** All `src/sessionorc`, which your 17:0Z instruction reserved. **#307** TD-087's host-agent half: the usage poll says *why* it has no reading (`rate_limited` with the endpoint's `Retry-After`, `no_credentials`, `no_profile`, `error`), backs off on a 429 alone, keeps the last good reading across a restart in `usage.json`, and polls every five minutes rather than every minute — `tdgrind-ao-2`'s chip words have been blocked on it. **#315** TD-063: a client call that is never answered is an error naming the method rather than a wait without end — the mechanism `tdgrind-ao-2` found behind a hang leaving no evidence, and the half that matters is not the tests: a live worker sitting in `ao send` never came back while its lead read it as working. **#317** TD-083 step 1: `ao progress restart --why` — a worker can say *my run is over and my lane is not*, which is the thing that parked a team for ninety minutes on 2026-09-20; it unblocks that session's chip. **#318** TD-077 (b), Log TD: an alarm is handed to the record's first live controller as work that **owes an outcome**, which is the debt extension the design asked for first. **What is left of TD-077 is (c)**, the chain of command, which waits on TD-075 and never applies on a host that carries a person. Every one carries a `cadence-review:` comment, and three of the four had real findings fixed before this line was written — #318's was a correctness bug (a node's alarms cleared on the home's replica, which its next report undoes) and #315's was worse than the hang it fixed (a wedged agent laundered into *nothing changed*). Context: PRs #307, #315, #317, #318 / TD-063, TD-077, TD-083, TD-087. Due: 2026-09-21.
- [x] 2026-09-20 (session `tdgrind-ao-1` on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **Merged, live check pending: the Inbox is a queue (TD-079, all three steps) and `ao status -v` now says what a session owes.** Nothing here is broken; these are the things no session may check. After the next promote and a browser: the Inbox page shows the fourth section **Waiting on them** with an answered question, its answer and the asker's `doing` line, and **Dismiss** on a row ends the debt and tells the asker; the top bar carries a **second, smaller number** for FYI, which opens itself when something new lands and carries a *new* mark; **Dismiss all** clears what is on screen and nothing that arrived after the page was drawn; a state row's **Snooze** on `stalled?` or unpushed work takes it out of every section and count until its time; and a row that resolved by itself leaves a **trail** entry in FYI saying how (*allowed by you*, *resumed*, *forgotten*, *resolved*). From a terminal: `ao status -v` on a session that owes an outcome prints an `owed: m-…` line beside its other mail marks — the line a lead reads to chase one. The **briefs** (`src/agentorc/briefs/grinder.md`, `hunter.md`, `lead.md`, this repo's two) take effect at the next `ao team start`, not on the sessions running now: the first team started after the promote should have a worker that reports an outcome without being asked, and a lead whose round step 2 chases one. Context: TD-079 (archive) / PRs #267, #269, #287, #295. Due: 2026-09-23.
- [x] 2026-09-20 (session `tdgrind-ao-2` on kmaster) — **Handled 2026-09-23 (the anchor): samscrape stood its team up from `ao team --skill` on 2026-09-22 — that was the live test.** As written: **TD-067's *done when* is a live test, and only you can run it: does `ao team --skill` actually stand a team up?** The recipe is written and merged (PR #302): `ao team --skill` prints it, it needs no host agent and no checkout, and every command and config key in it is asserted against the loader that reads it. What no test can show is the thing the entry asks for — give a **fresh** Claude Code session on kmaster nothing but `ao team --skill` and the sentence *stand up a grind team for contractmatch*, and see whether it writes the definition, starts the team and reports `ao team status` **without reading design.md**. Where it reaches for the design instead, that gap is the next edit to the file. (Your own path is the same text from the README's *Stand up a team*.) Context: TD-067 / PR #302 / `src/agentorc/team_skill.md`. Due: 2026-09-24.
- [x] 2026-09-20 (session `tdgrind-ao-2` on kmaster; five lines folded into one 2026-09-20; updated at wrap-up) — **Folded 2026-09-23 into the one browser walk below.** As written: **One pass over the UI: everything I built this run, in the order the page takes it.** Five *live check pending* lines each asked you to open the same UI; they are one walk. All five PRs are **merged** (#279 TD-073, #281 TD-082, #287 TD-079 step 2, #290 TD-081 step 2, #292 TD-085); **whether each has been promoted is the anchor's to say and I have not verified it** — a line of `tdgrind-ao-1`'s above still lists TD-079 as awaiting the next promote, so do this after one. Every rule below is held by a test; what is on this board is **how it reads**, which no unattended session may judge. **(1) The top bar:** each profile's chip prints its **worst** window (`grind wk 88%`) with both on hover; narrow the window until the chips stop fitting and the lowest fold into **+n** (names and windows on its hover), a profile at or above 80% never the one folded — **note TD-087: the endpoint is answering 429 and the chip may be empty, in which case this part waits for that fix**. Beside it, does *Inbox 1 · 5* read as two numbers or as one? **(2) `/inbox`, wide:** one centred column; sections are headings and rows are cards; press the **i** on each section including the one inside FYI's heading, **which must not fold the section**; tab through a row for the focus ring; **marks flat and controls bordered**; and no `…` anywhere a time is shown. Then the queue: *Waiting on them* as a fourth section; a **`blocked`** outcome and an **asker that exited without reporting** each counted under *Needs you*; a **`done`/`dropped`** outcome shown under the question it closes; **FYI opening itself** when it holds something new, with a **new** mark on those entries; **Dismiss all** confirming with the right count; a **Snooze** on the two state rows that are not on the tool's clock (`stalled?` and unpushed work), and its Unsnooze; and whether a **trail** row tells you enough about what went away. **(3) Focus at 1440 and at 1280** — TD-085's two widths — on a session carrying a title, a stop time, a team, a role, grants, a controller and a doing line: the name must read **whole**, its state beside it, every chip present; if it still reads badly, splitting the header into an identity line and a control line is a §4.5a decision and yours. **(4) The one that is an act, not a look:** resume `orchestrator-ao-1` from its Focus page with **one press** and confirm you get **one** record under its own name — not a second beside it, which is what happened on 2026-09-20 — with its inbox intact and **attended** whatever it was; then **Resume with changes…** beside it reaches the form filled in, and the Inbox's *exited with unpushed work* row offers **Reopen and push**. **(5) Added at wrap-up — the marks since (#306, #312, #323):** suspend a session from its alarm row and see the flat **suspended** mark on its card, its Focus header and the row, the row still standing; and on a session that runs `ao progress restart --why`, the **restart wanted** chip on card and Focus — appearing on an **open** Focus page without a reload — reading *restart wanted · early* in the needs-you colours if it asked inside its first half hour. Context: TD-073 / TD-079 step 2 / TD-081 step 2 / TD-082 / TD-085, PRs #279 #281 #287 #290 #292 #306 #312 #323. Due: 2026-09-23.
- [x] 2026-09-20 (session `orchestrator-ao-1` on kmaster; updated 2026-09-21 by the ao-grind lead of run 2) — **Decided by TD-093 (Paul, 2026-09-22): the anchor's hand is `src/sessionorc/`, `docs/briefs/` and `org.yml`; everything else merges on the cadence check. Closed 2026-09-23.** As written: **Two PRs are waiting on you, and while they wait a worker has nothing to build: how wide should the anchor-merge hold be?** *Handled since this line was written:* PR #269 (TD-079 step 1b) merged 2026-09-20 as 2355e86 and passes `scripts/check_cadence.py`, and both of run 1's sessions exited clean — nothing of run 1 needs anyone but the parked branch below. (Run 1's idling had a different cause, a steer of the lead's that lapsed at its bound, not this.) *The live part, from run 2 (2026-09-20 20:08 onward):* what you actually said, as the lead's log of 17:56 records it, is that **mail/identity PRs (the TD-079 series, #265) are held for the anchor to review and merge**. The workers have since applied that hold to everything under `src/sessionorc` — that broadening is theirs and mine, not yours, which is exactly why the width is worth one word from you. Under it, `tdgrind-ao-1` opens such PRs faster than they are merged: #297, #301 and #303 sat green and reviewed until you cleared them at 00:27, #315 and #317 until 02:32, and **#307 (TD-087, the usage poll's backoff) has been open since 00:46 and #318 (TD-077 (b), Log TD) since 02:14.** The cost is small but measured: `tdgrind-ao-2` sat idle from 02:09 to 02:29 because both of its next picks (TD-087's chip words behind #307, TD-083's restart-wanted chip behind #317) are page halves of fields those PRs create. `tdgrind-ao-1` never stopped, and ao-2 did not idle twice — after the lead's 20-minute nudge it found TD-029 in its own lane, put in writing that a Monitor on `origin/main` is its **only** wake signal for those two PRs, and named both blocked items with the PR each waits on rather than dropping them. **One word clears it:** confirm the hold is just mail/identity (and the rest merges under the ordinary cadence), or say it should stay as wide as the workers have made it and a worker idles whenever you are away. Merging #307 and #318 clears today's instance either way. Everything merged this run — 29 member PRs — passes the cadence check; the only fail was #309 (an evidence comment edited 18 s after its merge, and no independent reviewer), which `tdgrind-ao-1` owned and remedied in a second comment plus #313. **Also still waiting on you:** branch `td072-mail-before-winddown` (0c995c9, still on origin, unmerged), a complete build of TD-072 dropped on your word for overlapping TD-079 and named in TD-072's Status. Context: PR #307 / PR #318 / branch td072-mail-before-winddown / design §4.8, §4.9a / lead log docs/briefs/orchestrator-ao-1.log 17:56 and 02:09-02:33. Due: 2026-09-21.
- [x] 2026-09-20 (session `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending: the outcome debt, the pushed measure and the Inbox poll.** They reach the live system at the anchor's next promote (the outcomes half of TD-079 was promoted already, and reporting an outcome works — this session used it). Then: on the Org, a session's card that owes an outcome says so on every `ao` reply and its Ready to close shows *outcomes reported (n owed)*; `ao progress none` is refused while one is owed; the manager's own worktree (`orchestrator-ao-1`, ahead of `origin/main` and pushed to its own branch) shows **no** *unpushed* flag and no *exited with unpushed work* row, where it read *308 unpushed* before; the Inbox page with the host agent stopped shows the banner and keeps its rows instead of blanking them, and the top bar's count goes blank rather than 0. The **attention trail** is not in this: it is PR #269, still open, and it is the host agent's half only — nothing on a page renders a trail until TD-079 step 2, which waits on TD-082's design round, so there is nothing to look at for it yet. Context: TD-079 step 1a / TD-080 / TD-069, PRs #267, #270, #271 (#269 open). Due: 2026-09-22.

- [x] 2026-09-18 (session 5b560f5e on kmaster) — **On hold with the guardians setup, Paul 2026-09-23 — the hold line below.** As written: **Merged, live check pending: TD-057 step 4b — a node's pane read across the link, a permission answered over it, the home's intent pushed to the node, a team's roles read on a machine node, the nightly tarball (PRs #222, #223, #224).** Reaches the live system at the anchor's next promote (it restarts client and agent together). Then, with the contractmatch container node linked: from kmaster `ao tail <id>@contractmatch` and `ao explain <id>@contractmatch` print its pane; a permission prompt in a grinder there shows *needs you* on kmaster's card and Allow there reaches it; `ao msg <id>@contractmatch "x"` and then, inside the container, that session's `ao status` carries the unread line; should the link drop mid-prompt, the card reads *host unreachable — answer it at contractmatch* (not worth forcing: `docker stop` would end the session itself). The day after the promote, `ls -l ~/.agentorc/backups/` on kmaster shows one `store-<date>.tar.gz`, mode `-rw-------`, and `tar tzf` on it lists no `nodes/` and no `runs/`. With a machine node (guardians' VM) linked, `ao team start` of a team whose `host:` is that node reads its briefs there with no clone on kmaster. Context: TD-057 / PR #222 / PR #223 / PR #224. Due: 2026-09-21.
- [x] 2026-09-17 (session d736f01f on kmaster) — **On hold with the guardians setup, Paul 2026-09-23 — the hold line below.** As written: **The container node's one-time steps.** (The promote is the anchor session's, per CLAUDE.md: it promoted through #214 on 2026-09-18 and promotes again after #215 merges — not yours to run.) For contractmatch as a node: (1) `nodes: {contractmatch: {container: {devcontainer: ~/contractmatch}}}` in `~/.agentorc/hosts.yml` and a restart of the agent (the link socket is bound at start); (2) in contractmatch, re-point `setup_scripts/postCreate.sh` (and the Dockerfile's `WORKDIR`, `safe.directory` lines, `.claude/settings.json`, `gen_og.py`) from `/workspaces/contractmatch` to `/home/kmaster/contractmatch`, and pin `useradd --uid 1000` — the live check died exactly there; (3) `~/.agentorc/nodes/contractmatch/env` (`0600`: `GH_TOKEN` scoped to contractmatch, `GIT_AUTHOR_NAME/EMAIL`, the Doppler service token); (4) `ao host up contractmatch`, then one `claude` login inside for the `grind` profile (`docker exec -u developer -it <id> …` with `CLAUDE_CONFIG_DIR=/agentorc/profiles/grind`); (5) **after step 4a's promote** (PR #213, merged 2026-09-17, live check pending): `contractmatch: ~/contractmatch` beside `kmaster:` in the project's repo entry and `host: contractmatch` on `cm-grind` in `~/.agentorc/org.yml`, then `ao new --host contractmatch -d ~/contractmatch --unattended w` from kmaster and `ao send`/`ao kill` on the `…@contractmatch` id it prints — the first acts across the link on a real node — and `ao team start cm-grind` as the done-when; (6) 3c.4's live check (PR #215): with a session inside holding `~/contractmatch`, `ao new` there on kmaster is refused by the anchor rule, and the reverse with `--host contractmatch`; (7) 3c.5's (PR #216): once the node is linked, its card shows a VS Code link that attaches to *that* container, and Focus on a session inside gives a live terminal (scroll too), as does `ao focus <id>@contractmatch`; (8) step 5's (PR #217): from inside the container `ao msg <lead-id>@kmaster "…"` lands in the lead's inbox here and `ao inbox` there reads the reply; `ao msg` to the container's session while its link is down prints *landed — host unreachable* and the mail is read when it returns. The 4.6 GB base image from the scratch run is kept so the real `up` is cached; `docker rmi agentorc-node-contractmatch-base` if the root disk needs it (19 GB free after the build). Context: TD-057 3c.2 / PR #211. Due: 2026-09-20.
- [x] 2026-09-17 (session `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Three merges tonight only reach the live system at the next promotion, and one of them has a one-time edge: the running host agent predates the skew rule that protects it.** `pip install --upgrade` + `ao service install` per CLAUDE.md. (1) **TD-062 (a)+(b), PR #197:** `ao` no longer sends a parameter it has not set, and the host agent now drops a parameter its method does not take instead of refusing the call, saying so in one line. From the *next* promotion on, a merged RPC change cannot break `ao` for every worker the way PR #180 did for 31 minutes — but the agent running now predates (b), so this promotion is still a hard cut like any other. (2) **TD-063, PR #199:** a real host-agent bug — a tick holding a pane list older than a `kill` revived the record it named, so an `exited` session read `idle` and then refused its own `remove` with *kill it first*. Live until the promotion. (3) **TD-053 step 6, PR #203:** the **out of work** chip on the card and Focus header and the Teams strip's *wound down <t>* — UI, so it needs the UI restarted to be seen, and it wants your eye rather than a check: the chip's reason is a hover, and whether that is enough is a judgement no test makes. Context: TD-062 / TD-063 / TD-053, PRs #197 #199 #203. Due: 2026-09-19.
- [x] 2026-09-17 (session 192b2355 on kmaster) — **Decided (TD-064, built 2026-09-22): unattended sessions refuse Claude Code peer messages; its live check is the 2026-09-22 line below. Closed 2026-09-23.** As written: **Choose a policy for Claude Code's own session-to-session messages into agentorc sessions: deny (mail stays the only channel) or accept (your shortcut keeps working, outside the mail gates).** Your samscrape session's message to `orchestrator-sam-1` was held for approval and left the team without a lead until it was answered by hand; an unattended session cannot answer that dialog. Until you choose, use `ao msg <lead>` or the card's **Message** to tell a lead something. Context: TD-064. Due: 2026-09-24.
- [x] 2026-09-17 (session 192b2355 on kmaster) — **Confirmed by Paul 2026-09-23: both rules stand as §4.4a writes them.** As written: **Confirm two decisions TD-057 step 2 wrote into design §4.4a, which the rule there had not made: (1) on a node that cannot reach its home, `ao progress` and `ao finding` are *refused*, not kept on the replica — reports are home-owned, and a claim is a lease checked against siblings the node cannot see; (2) a person's mail from an offline node is refused too, because the inbox is not on that machine.** Neither has any effect until a `hosts.yml` carries a `home:` line and none does; reversing either is one line in `sessionorc.modes` and a row of the table. Context: TD-057 / PR #198. Due: 2026-09-24.
- [x] 2026-09-17 (session `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Restart the host agent (and UI) before the next `ao team start`, `ao new --role lead` or `ao grant … control`: the `orchestrate` grant is now `control` (TD-055 step 3).** Once you pull, `ao` sends `control`, which the running pre-rename host agent refuses as `unknown grant control`; after the restart every stored record holding `orchestrate` is rewritten as `control` on load (the agent logs one line per record) and both names are accepted for one release. Nothing already running is affected — the gate only checks a grant when a session acts. Session and brief names (`orchestrator-ao-1`, `guardians-orchestrator`) are TD-055 step 4, yours, untouched. Context: TD-055. Due: 2026-09-18.
- [x] 2026-09-17 (session `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **The `orchestrator` role is now `lead` (TD-055 step 2); your `~/.agentorc/org.yml` still says `role: orchestrator` for both team leads and has a `roles: orchestrator:` overlay.** Both keep working for one release through the alias, and each `ao` run that reads them prints `role \`orchestrator\` is now \`lead\``. When convenient, rename `role: orchestrator` → `role: lead` and the `roles:` key `orchestrator:` → `lead:` (session names such as `orchestrator-ao-1` are step 4, yours, and unaffected). Context: TD-055. Due: 2026-09-24.
- [x] 2026-09-17 (session `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **The ao-grind briefs now tell each member to declare itself out of work (`ao progress none --why`) before exiting; that verb needs an agent restart (PR #178), and the briefs take effect on the next `ao team start ao-grind`.** Until the restart a member that tries it is refused (observed 2026-09-17 on the live agent: `bad params: HostAgent.rpc_progress() missing 1 required positional argument: 'ref'`) and is told to note that and exit anyway, so the lead will still read that exit as a crash. Nothing was sent to the running lead or workers. Context: TD-053 steps 1–2 / PR #178, PR #179. Due: 2026-09-20.
- [x] 2026-09-17 (session `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending: the host agent now stops in about a second with the UI open and a lead blocked in `ao wait` (TD-058, PR #176).** Next time you restart: `time systemctl --user restart agentorc-agent` with the UI running should return in a few seconds, and `journalctl --user -u agentorc-agent` should show a clean `Stopped`, no `stop-sigterm timed out` and no SIGKILL. PR #177 also fixes the UI's own 40 s stop: `time systemctl --user stop agentorc-ui` with a browser tab open should return in about a second. The restart that this line asks for turned on **claim leases** (TD-056, PR #180), and **that half is now done — nothing here for you** (2026-09-20): with `tdgrind-ao-1` holding `TD-078`, `tdgrind-ao-2`'s `ao progress claim TD-078` was refused on the installed build — *claimed by ao-agentorc-tdgrind-ao-1 since 2026-09-20T20:43:08Z (a lease, design §4.8): pick another reference, or claim it anyway with `--force`* — so the at-claim `note` came out of the grinder briefs in PR #291 and TD-056 is archived. (The agent has been restarted many times since, by the promotes; what was never done is TD-058's *timed* check below.) **What is left on this line is TD-058's**. Context: TD-058 / PR #176, PR #177; TD-056 / PR #180. Due: 2026-09-20.
- [x] 2026-09-16 (session 746e1973 on kmaster — a samscrape session; branch `td059-prior-art-survey`) — **Decided 2026-09-21 by Paul: `ShiftLead`, shiftlead.dev bought, `ao` stays the command (TD-060 carries the order of the rename).** As written: **Decide the project's new name: `shiftlead` leads.** `agentorc` is taken. `shiftlead` passed every check (PyPI, npm, crates.io, GitHub org; you found `.dev`/`.org`/`.tech` open) and matches the glossary's `lead`; you asked for time to think. The ADR lists five more decisions that follow the name (what happens to `sessionorc`, whether the `ao-` id prefix stays, an `ao` alias, a compatibility window, the order against TD-055). **Both names are now claimed (2026-09-17): PyPI `shiftlead 0.0.0` (a placeholder, https://pypi.org/project/shiftlead/) and the GitHub org `ShiftLead` — so the urgency is gone and the decision can take the time it needs; the domains (`.dev`/`.org`/`.tech`) are still yours to register.** Context: TD-060 / TD-059 / [ADR](decisions/2026-09-16-rename.md). Due: 2026-09-23.
- [x] 2026-09-16 (session `td052-step3` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending on the running agent: `ao wait` is now the host agent's `wait` RPC, and the running lead's `ao wait` fails (one line, exit 1) until the agent restarts.** After the restart: from a lead session run `ao wait --timeout 60` in one pane and, from a member, `ao msg <lead> "test"` — the wait should return with `mail: 1 new … run ao inbox`, and every `ao` command the lead runs should end with `[agentorc] you have 1 unread messages — run ao inbox` until it runs `ao inbox`. `ao status -v`'s record now carries `wakes` (what step 5 measures). The wake budget is unlimited until step 6. Context: TD-052 step 3 / PR #171. Due: 2026-09-18.
- [x] 2026-09-16 (session `td052-step4` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **The ao-grind briefs now carry the mail rules; they take effect on your next `ao team start ao-grind`, and nothing was sent to the running lead or workers.** TD-052 step 4 edited `docs/briefs/orchestrator-ao-1.md`, `docs/briefs/tdgrind-ao-1.md` and the three preset briefs: the lead reads `ao inbox --unread` at the top of its round and answers every `ask`; a grinder notes its siblings at claim and mails its lead at `done`. Restart the agent first (the item above for PR #166), or every `ao msg` answers *unknown method* and the briefs fall back to `ao progress`. `ao wait` returns on mail only once TD-052 step 3 lands. Context: TD-052 / PR #170. Due: 2026-09-19.
- [x] 2026-09-16 (session `td052-step8` on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **Merged, live browser check pending: restart the agent and the UI, then look at the mail controls.** TD-052 step 8 added the Focus **Inbox** panel, the card's **unread** chip, **Reply**, and **Message** (card `more ▾` and the Focus header). The tests drive the routes but no browser has run the new JS. Check: open Focus on a session, click ✉ Message and mail it a note; the card's ✉ 1 chip should appear and link to the panel; Reply and Delete on an entry; opening the panel must leave the count unchanged. Also the Org top bar's **person inbox** (PR #168, `td052-step8b`): have a session `ao msg person "…" --kind ask`, reload the Org — ✉ Inbox shows 1 — open it, Reply (the sender's `ao inbox` gets it, the ask closes) and Delete; opening it must leave the count unchanged. Context: TD-052, PR #165, PR #168. Due: 2026-09-18.
- [x] 2026-09-16 (session `td052-step2` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending on the running agent: `ao msg` / `ao inbox` and the person inbox.** The live agent runs code older than PR #166, so after the next agent restart: from a session run `ao msg person "test"`, then at a plain terminal `ao inbox` (the fixed header, the entry marked `[other]`) and `ao msg --reply-to <id> "ok"`; in that session `ao inbox` shows `[person] person`. `~/.agentorc/person_inbox.json` should exist and survive a restart. Context: TD-052 / PR #166. Due: 2026-09-20.
- [x] 2026-09-14 (session e2213351 `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **`ao wait` exists; the lead briefs do not use it yet, and that is the part that wants your eye.** TD-049 steps (1)–(4) merged (#145): `ao wait --timeout N` blocks until a session whose `controllers` name the caller changes — a `done` with a PR, a state change, a new finding — and returns the changed records, so a lead's tick can end in it instead of a `ScheduleWakeup`. What is left is steps (5) and (6): the fallback interval, the short list of things a lead must do on the timer *regardless* of events (a merged PR from a worker's branch, a new `docs/cadence-changes.md` entry, a worker gone quiet — silence is not an event), and the `loop` instruction in `docs/briefs/orchestrator-ao-1.md`. I stopped short of editing that brief because it changes how the live lead behaves on its next restart. Suggested check after a restart: from the orchestrator's pane, `ao wait --timeout 60`, then mark something `done` on a worker and watch it return in about a second. Context: TD-049, PR #145. Due: 2026-09-19.
- [x] 2026-09-14 (session e2213351 `tdgrind-ao-1` on kmaster) — **Handled 2026-09-23 (the anchor): the `/cadence` skill and cadence §4 now say the verdict is the comment's first line.** As written: **`check_cadence.py` reads the `cadence-review:` verdict only on a comment's *first* line, and nothing says so.** `scripts/check_cadence.py` matches `REVIEW_RE` against `body.splitlines()[:1]`, so a comment that ends with the verdict line — which is how every reviewer is briefed to *write* its report, and how I wrote the evidence for PRs #138 and #139 — reads as **no review at all**, indistinguishable from a skipped one. Both PRs failed on that, not on a missing review. Two things worth your call: (a) whether cadence §4 should say "first line" where it gives the format, since a detector rule nobody can see is one every new session trips once; (b) whether the matcher should scan the whole body, which costs nothing and removes the trap. `scripts/` and `docs/cadence.md` are dev-cadence's, so neither is mine to change. Context: PRs #138, #139, #141; TD-050. Due: 2026-09-18.
- [x] 2026-09-14 (session e2213351 `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending on the running agent/UI (restart both):** TD-042 (#139) makes `ao team start` warn when a brief it is about to hand out names a clock time or a run number — worth watching that a real `ao team start ao-grind` prints *nothing*, since every brief in the repo is now clean, and that the Teams strip toasts it if you point a team at a stale brief. TD-028 step 5 (#141) draws the **Grants** checkboxes on New session: open the form, pick the `orchestrator` role and confirm the `orchestrate` box ticks itself with its warning line, then untick it and confirm the started session has no grant (`ao status -v`). TD-047 (#142) makes the Focus composer say which of Send's two jobs it is doing: open Focus on a **working** session and the button should read **→ Steer** with "steers the turn in flight"; on an idle one **→ Send** with "starts a new turn"; on a killed one the composer should be **disabled** with a line pointing at the banner below it. TD-026 (#147) makes that **stops** badge on Focus the control that edits it: click it for a time (`06:00`, `+8h`, an ISO time), empty to clear, and an unattended session with none shows a dim *no stop time* — the thing a person most needs to know and could not see before. One thing worth pressing deliberately, to **confirm a fix rather than find a bug**: press OK without changing anything while a session is inside its wrap-up grace. Before #147 that asked the session to wrap up a second time and deferred the forced kill; it is fixed there (only a *different* time counts as a new run) and covered by a test, so the expected result is that nothing happens. None of the four was checked in a browser — no session in this run has one, which is also why TD-029's last step is still open. Context: TD-042, TD-028, TD-026, TD-047 (archive), PRs #139, #141, #142, #147. Due: 2026-09-17.
- [x] 2026-09-14 (session 0675b93e `orchestrator-ao-1` run 3 on kmaster) — **Handled 2026-09-20 (`tdgrind-ao-1`, TD-090 in the archive): `translate()` reads `SessionStart`'s `source` and a `compact` changes no state, so an idle session stays `idle` through a `/compact`; merged, live check pending — the next `/compact` on a live worker should leave its card `idle`, not `stalled?`, after the next promote.** **A `/compact` leaves a healthy session reading `stalled?` forever, and §6's stall policy would wrap it up for it. No ledger entry holds this.** Observed live on `ao-agentorc-tdgrind-ao-1` at 14:58Z: state `stalled?` (confidence `scraped`), `ao explain` giving no rule and no evidence, `last_hook` 14:28:28Z — while the pane showed a **completed** `/compact` ("Compacted (ctrl+o to see full summary)", five files listed below it) above an **empty composer**. That session is `idle` in substance: every lane item done or dropped, tree clean, nothing running. **The mechanism, from the code:** `translate()` (`src/agentorc/adapters/claude_code/hook.py:43-87`) passes `SessionStart` straight into `STATE_EVENTS` → `working` (`hook.py:24-28`) with **no look at the payload's `source`** — and Claude Code's documented compaction sequence ends by firing `SessionStart` again with `source: "compact"` (`PreCompact` → `PostCompact` → `SessionStart(compact)`; the hooks guide's own "re-inject context after compaction" recipe matches on exactly that). Neither `PreCompact` nor `PostCompact` is in `HOOK_EVENTS` (`adapters/claude_code/__init__.py:30-42`), so nothing ever reports the compaction finishing; the record sits `working` until the liveness cross-check at `agent.py:556-559` flips it to `stalled?` at `STALL_AFTER`. I did not capture the 14:28:28Z payload, so *which* event set `working` is inference — but it is the only registered event the documented sequence fires, and `/compact` is a built-in that most likely never reaches `UserPromptSubmit` at all. **The shape of the bug is already solved next door in the same function:** `SessionEnd` is checked against `SESSION_END_STILL_RUNNING` (`{"clear", "resume"}`, `hook.py:31`) precisely because some lifecycle events are not what they appear — `SessionStart`'s `source` wants the same treatment, or `PostCompact` wants registering as the explicit "compaction over" signal, or both. **Why it is worth your time rather than a curiosity:** auto-compaction is routine for exactly the long-running unattended workers this system exists to watch, so every one of them will eventually read `stalled?` while perfectly healthy. Today that only misleads a human reading the Herd and an orchestrator following its brief (I did not nudge — the tail shows output and the session holds no open reference, so the prescribed nudge had nothing to say). But §6's stall policy is **"`working` with no output past `stall_after` → flag `stalled?`, nudge once, then wrap up"**, and when that becomes agent code it will nudge and then wrap up a session whose only sin was compacting its context. **Sharpened 15:32Z, and it is worse than first written: the mislabel does not clear itself.** At 15:32Z the record still read `stalled?` with `last_hook` unmoved at 14:28:28Z, even though `last_output` had advanced to 15:26:06Z on an otherwise unchanged screen. The reason is that the liveness check is a one-way door: `agent.py:559` is the only assignment of `stalled?` anywhere, and its branch is guarded on `s.state == "working"`, so once the record leaves `working` that branch is structurally unreachable and no amount of fresh `last_output` can bring it back. Two paths *can* clear the state, and neither fires here: any hook carrying a mapped state reaches `_apply_event` (`agent.py:574-594`), whose `set_state` is unconditional — `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, `SessionEnd` and `Notification` would all clear it — and the screen fallback (`_screen_verdict`, `agent.py:552-555,566-571`) would clear it if a rule matched, since it is not guarded on the current state. But a session resting at an empty composer emits no hook at all, and `ao explain` answers `match: null, reason: "no screen rule matched"` because no rule covers an idle composer. So the mislabel is not transient: nothing the system does on its own corrects it, and in practice it clears only when somebody submits a prompt — a submitted prompt being the only one of those events an idle session is likely to produce. That is precisely the window a supervisor acts in, which is what turns a cosmetic wrong badge into a policy that would nudge and then wrap up a healthy worker. Needs your call on whether this becomes a TD (an orchestrator never files its own) and on the fix shape. Not acted on: the sub-agent that confirmed the hook semantics suggested adding the handler to this repo's `.claude/settings.json` — that is both outside my brief and the wrong layer, since agentorc's hooks reach a session as a generated `--settings` layer (design §4.2), never through a repo's own file. Context: `src/agentorc/adapters/claude_code/hook.py:24-31,43-87`, `adapters/claude_code/__init__.py:30-42`, `src/sessionorc/agent.py:548-559`, design §4.2 (hook → state table) and §6 (stall policy), Claude Code hooks reference (`PreCompact`/`PostCompact`, `SessionStart` matcher `compact`). Due: 2026-09-15.
- [x] 2026-09-13 (session e2213351 `tdgrind-ao-1` on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **Merged, live check pending on the running agent/UI (restart both), and two of these want your eyes rather than a restart.** (1) **A stop time for unattended sessions** (TD-026, #129 + #131): `ao new --unattended --until 06:00|+8h`, `ao until <session> <when>|--clear`, *stops 06:00* on the card, the Focus header and `ao status -v`, and an **Until** field on New session. At the time the agent sends the wrap-up prompt once and stops the session when it settles or ten minutes later — so the thing to watch is a worker that *does not* stop — the wrap-up never landing, or the ten minutes passing without a kill — rather than one stopping early, which the code guards structurally. This is the gap that left hand-started workers running with nobody to close them. (2) **A stood-down worker reads `stalled?`** (TD-032 a/c, #125), from a screen rule written off the quoted banner in TD-032 rather than a captured pane — **if you ever see a real Remote Control stand-down, save the pane**: the rule needs both the close code and a `/rc failed` footer, and only a real screen can say whether that pair is right. (3) **The Focus terminal now uses VS Code's Dark Modern palette** (TD-038 b, #132) — open a session in Focus beside the same one in VS Code's terminal and see whether the colours match; the font and the WebGL renderer are still open, and (c) needs the vendored xterm.js's version identified first. (4) **The mockups are current again** (TD-037, #127), but nothing rendered them: `docs/mockups/shot.sh` needs a Chromium and none of the sessions in this run had one — worth one run and a re-seed of the canvas. Context: TD-026, TD-032, TD-037, TD-038; PRs #125, #127, #129, #131, #132. Due: 2026-09-17.
- [x] 2026-09-14 (session 0675b93e `orchestrator-ao-1` run 3 on kmaster) — **Folded 2026-09-23 into the dev-cadence line below (Paul: file all three there).** As written: **The cadence check's `review` row, once failed on a merged PR, can never be cleared — the only honest response leaves a permanently red row, and my brief's "second fail on the same PR" escalation fires on it whether or not the worker did anything wrong. No ledger entry holds this.** Observed on agentorc **PR #128** (`ledger-board-tdgrind`, a one-line board append, merged 01:42:48Z): the row read `no cadence-review: comment on the PR`, I sent `tdgrind-ao-1` the single nudge the brief prescribes, and it did the best thing available — at 01:42:58Z it posted a note recording that the review had *not* happened before the merge rather than back-filling a claim that it had, then at 01:53:33Z posted the evidence comment itself, labelled `(post-merge)`, verified the board line's every claim against main, and stated that a board-line PR is not exempt and every future PR gets its comment before the merge. The re-check still fails, now with `review comment posted after the merge (2026-09-14T01:53:33+00:00 > merge)`, and it will fail forever: `row_review` compares the comment's `created_at` to the merge time, and GitHub stamps both, so no action by anyone can make this PR's row green. That is the intended design — §4's whole point is that a timestamp cannot be rewritten — but it has a consequence nobody has written down: **a missed review is an unclearable state, not a fixable one**, so the check's per-PR verdict and every window sweep (`--since 7d`, `/stranded-work`) will carry this PR as a fail indefinitely. The same shape as the immortal `derived` claim this orchestrator filed on 2026-09-12, arriving from the other direction — there, a claim that could never be retired; here, a fail that can never be cleared. That one has since been **fixed and closed** (TD-045, PR #126, merged by `tdgrind-ao-1` earlier in this same run: a claim that resolves to `done` at the merge is no longer immortal), so it is offered as the precedent for how this class of problem gets settled, not as a live sibling. **Why it needs you rather than a worker:** the worker's behaviour was correct and a second nudge would punish honesty — a worker that learns the row is the thing being graded will back-date or edit a comment to clear it, which is precisely the failure §4 exists to prevent. So the question is about the *detector*, your call: should a fail whose cause is in the past be expressible as **acknowledged** (a distinct status the sweep can skip, e.g. a follow-up comment the row reads), or should the window sweep report unclearable fails separately from actionable ones, or is a permanently red row the correct and intended cost of a missed review? Related: it is also the brief's escalation rule that needs the answer — "a second fail on the same PR escalates" cannot distinguish a worker ignoring a nudge from a worker that answered it perfectly, which is the only reason this line exists. Not nudged twice; the worker's own commitment ("every PR from this session gets its evidence comment before the merge from here on") is the forward fix and needs nothing from you. Context: PR #128 and its two comments, `scripts/check_cadence.py` `row_review`, cadence.md §4 and §7, docs/briefs/orchestrator-ao-1.md (escalation rule), design §4.8. Due: 2026-09-14.
- [x] 2026-09-13 (session e2213351 `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending on the running agent/UI (restart both):** TD-043 (#122) gives every `ao send` its own tmux paste buffer — the fix for the orchestrator's `no buffer ao-paste`, so it can stop serialising its sends; TD-045 (#126) retires a derived claim whose branch never grew a PR, so a worker that abandoned a branch stops reading as having open work (watch that no *live* claim disappears — the retirement asks `gh` per branch and keeps everything on an outage). TD-044 (#123) is test-only. The one-line thing to watch: after the restart, does any card's `report:` line lose an entry it should have kept? Context: TD-043, TD-044, TD-045 (archive), PRs #122, #123, #126. Due: 2026-09-16.
- [x] 2026-09-13 (session b4f95a9e, the anchor, on kmaster) — **Handled 2026-09-22: samscrape has `.agentorc.yml`, `docs/briefs/` and a running team. Closed 2026-09-23.** As written: **samscrape has no `.agentorc.yml` and no `docs/briefs/`, so its team is defined but deliberately not started.** `~/.agentorc/org.yml` now defines two teams (design §4.9): `ao-grind` is **running** (orchestrator-ao-1 over tdgrind-ao-1, both on the repo's own briefs), and `samscrape-grind` (an orchestrator over three grinders) is defined and stopped. Starting it today would run its grinders on the package's generic grinder template and lose the standing rules that lived in `~/.tdgrind/tdgrind-*-prompt.md`: the avoid-list, the CRLF files, no cluster deploys, the coordination rule between siblings. Those are samscrape's judgement, not an agentorc session's to write across repos, so the ask is: have a samscrape session add `.agentorc.yml` (at least `roles:` with a grinder brief, `ledger:`, `controllers:`) and `docs/briefs/` carrying those rules, then `ao team start samscrape-grind` here. Until then samscrape has no workers at all — the four that ran until tonight were killed as part of the rebuild. Context: design §4.9, TD-040, `~/.agentorc/org.yml`. Due: 2026-09-17.
- [x] 2026-09-10 (session 9b6d2ce8 `tdgrind-ao-1` run 3 on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **TD-027 merged, live check pending — and the four "stuck composers" need no Enter:** the three samscrape composers show Claude Code's *faint* suggested-next-prompt text (read-only `capture-pane -e`), not an unsubmitted prompt — nothing ever sent them a wrap-up (they run under agentorc with no supervisor: TD-026 gap 1), so close them however you close a finished worker; `ao-agentorc-agentorc-tests-2` is a dead pane that exited on `/exit` on 2026-09-09. After restarting the agent, one `ao send --wait <id> …` to a Claude session should return normally (`prompt-stuck` if the composer keeps the text); `ao send` is ~0.2 s slower per call (it waits for the paste to paint). Context: TD-027 (archive), PR #60. Due: 2026-09-13.
- [x] 2026-09-10 (session 31a6d882 `tdgrind-ao-1` on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged overnight, live check pending on the running agent/UI (restart both to pick them up):** `limited` from the usage endpoint + the top-bar usage chip (TD-001, #44); "finished · unseen" idle cards (TD-017, #43); `ao send --wait` (TD-016, #42); screen rules — a fresh session's trust dialog should show `needs-you (scraped)` within a tick, `ao explain <id>` shows why (TD-015, #47); `ao new --attach` / `ao focus <id>` (TD-010 b, #46); `ao --json` everywhere (TD-018, #41); TD-009/011/012/013/020/021 fixes and TD-007 accepted. The one-line thing to watch: does anything in the Team look wrong after the restart? Context: docs/technical_debt_archive.md entries dated 2026-09-10. Due: 2026-09-13.
- [x] 2026-09-10 (session 38caa51c `tdgrind-ao-1` run 2 on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Merged, live check pending:** run 2 merged TD-024 (#51), TD-023 (#52: killed sessions offer Details, `ao focus` refuses cleanly), TD-004 minus ssh (#55: `hosts.yml` `local` entry takes `volatile`, `repos_registry`, `runs_keep_days`; the `AGENTORC_*_HOST` env vars are gone — if the agentorc-ui unit sets any, move them into the file), and TD-010 (a) (read-only *registry* cards for your VS Code `claude` sessions — the phase 1 success test's last gap: after restarting the agent every live session on kmaster should show with the right state within a tick; ledger anything wrong). `agentorc-agent serve` now stops by cancelling the serve task (TD-024) — after the next `systemctl --user restart agentorc-agent`, `journalctl --user -u agentorc-agent` should show no `Task was destroyed` ERROR line and the old socket file gone. Context: TD-024 (archive). Due: 2026-09-13.
- [x] 2026-09-11 (session 070fdec7 `tdgrind-ao-1` run 5 on kmaster) — **Verified 2026-09-23 (the anchor): promoted long since and in daily use.** As written: **Run 5 merged four items; restart the agent and the UI to pick them up, then two things to look at.** (1) `ao progress` / `ao finding` exist now (TD-028 step 2) but the *running* agent does not know the RPCs — this session could not declare its own progress, it got `unknown method 'progress'`; after the restart, `ao status -v` should show a `report:` line for any session that declares one, and the tick should fill in `derived` entries from branches and merged PRs within five minutes (step 3). (2) Every card should now show the model actually in use as the third part of its profile line (TD-031) — `claude-code · paul · opus-5`, or `… (profile)` until the first observation; a `/model` switch should change it within a tick. Also live: one name, one session (TD-030) — starting a name twice refuses with **Switch to**, an exited one is replaced with its run log kept, and `ao focus <name>` takes a bare name. Context: TD-028, TD-030, TD-031 (archived), PRs #69, #70, #72, #75, #77. Due: 2026-09-14.
- [x] 2026-09-11 (session 070fdec7 `tdgrind-ao-1` run 5 on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **TD-029 merged, one live browser check pending:** open Focus on a session and press **Close**. Expect one `[agentorc] this session's pane is gone` line and **no** further `/term/…` attempts in the UI journal (before the fix it reconnected twice a second until Forget). The server half is covered by tests; (a) and (c) are JavaScript, which this repo has no harness for — so this one needs your browser. Restart `agentorc-ui` (and the agent) first to pick the change up. Context: TD-029, PR #71. Due: 2026-09-14.
- [ ] 2026-09-12 (session b9d7bd29 `orchestrator-ao-1` run 2 on kmaster) — **`ao send --wait` reported `prompt-stalled` for a prompt that in fact ran; no ledger entry holds this.** At 2026-09-12 ~13:44Z the first of four relay sends to `ao-samscrape-td377-enumeration` returned `prompt-stalled: ao-samscrape-td377-enumeration showed no activity within 5 s` and exit 1. It had not stalled: `ao explain` immediately afterwards showed the turn **done at 13:45:25Z** (`last_hook` 13:45:27Z, state `idle` by hook) with a 27 s answer to that exact relay text on screen. The window is `SEND_STALL_SECONDS = 5.0` (`src/sessionorc/agent.py:55`), and `rpc_send`'s wait leg treats *any* record revision change as "started" (`agent.py:873-877`) — so the 5 s is the budget for the first hook of the new turn to reach the agent, not for the tool to begin thinking. The session was a long one (its pane footer read 372.1k tokens), which is the obvious suspect for a first hook later than 5 s, but the mechanism is not measured. Relays 2-4 to the same session minutes later returned `idle` normally, so it is intermittent, not a broken path. **Reproduced 14:55:45Z on a second session, and the long-context theory does not survive it:** the nudge to `ao-agentorc-tdgrind-ao-1` (up about an hour, nothing like 372k tokens) returned the same `prompt-stalled … within 5 s`, and the prompt had run — the record gained `TD-025 done #91 source: declared`, which only that prompt could have caused, with the turn settling at 14:55:50Z. So this occurrence is proved by its *effect*, not by a screen reading, and both a fresh short session and a 372k-token one hit it. Two in eight sends. Why it matters more than a cosmetic error: `prompt-stalled` is exactly the signal `ao --skill` and the orchestrator brief tell a caller to act on, and the sanctioned action after one is to look and then decide again — a caller that instead retried would paste a second prompt into a live turn, which is the TD-027 concatenation failure arriving through the front door. Handled here by verifying every send against `ao explain`/`ao tail` rather than its return value. Needs your call on whether this becomes a TD (an orchestrator never files its own), and on the shape of a fix — a longer or adapter-supplied window, or `prompt-stalled` re-checked against the composer being empty before it is raised. Context: `src/sessionorc/agent.py:55`, `agent.py:855-880`, TD-016 (archive), TD-027 (archive), design §4.2. Due: 2026-09-13.
- [x] 2026-09-12 (session b9d7bd29 `orchestrator-ao-1` run 2 on kmaster) — **Folded 2026-09-23 into the dev-cadence line below (Paul: file all three there).** As written: **The cadence check's `deploy` row answers from whichever clone it is run in, so a behind checkout turns a `fail` into an `na` — it silently suppressed a real finding on five samscrape PRs tonight; no ledger entry holds this (dev-cadence's, samscrape's, or agentorc's).** `row_deploy(pr, root)` (`scripts/check_cadence.py:364-370`, a dev-cadence SYNC file) tests `<root>/scripts/cadence_deploy_check.sh` in the *working tree* it is invoked from. samscrape PR #796 added that script (mode 100755, with a test) at 14:21:47Z, but `/home/kmaster/samscrape` is 6 commits behind `origin/main`, so from there the row reads `na — no executable scripts/cadence_deploy_check.sh` for every PR. Measured from a detached worktree at `origin/main` instead, the same PRs read **fail**: #796 `not deployed: dev1-samscrape-company-api-… has no /app/scripts/cadence_deploy_check.sh`, and #791/#792/#793/#794/#795 each `not deployed: /app content predates <sha> … — skaffold run`. So the row works and its verdict is correct; what is wrong is that its input is the invoker's checkout, and an orchestrator or a `/stranded-work` sweep runs it out of whatever clone it happens to sit in. A detector that reports `na` from a stale clone and `fail` from a fresh one is worse than one that always fails, because `na` reads as "this repo has no deploy gate" — which is what I logged at ticks 3-5 before measuring. Fix shape already exists in the same toolkit: dev-cadence TD-030's reader half has `nudge_user_attention.py --fetch` read a merely-behind clone's board from `origin/<default>` rather than the local file; the `deploy` row wants the same treatment (or an explicit `unknown` when the tree is behind its upstream, which is honest and cheap). The fix belongs in **eyecantell/dev-cadence**, not here and not in samscrape, because the script is SYNC. Not nudged to any worker: the five fails all say `skaffold run`, which is your action and is already asked for by two samscrape board items due today (its 2026-09-10 and 2026-09-11 entries, both `Due: 2026-09-12`), with several older lines overdue behind the same un-run rollout — the workers cannot clear it and deploying is outside every one of their briefs. The useful half of the news is that #796 makes those existing board items machine-checkable for the first time. Needs your call on whether this becomes a dev-cadence TD (an orchestrator never files its own). Context: `scripts/check_cadence.py:364-390`, samscrape PR #796, cadence.md §5 and §3, dev-cadence TD-030. Due: 2026-09-13.
- [x] 2026-09-12 (session b9d7bd29 `orchestrator-ao-1` run 2 on kmaster) — **Folded 2026-09-23 into the dev-cadence line below (Paul: file all three there).** As written: **The cadence check's `review` row names the *last* `cadence-review:` comment, so a PR whose author discloses its post-review fixes is reported as having reviewed itself — it reads as a §4 violation by a worker that did more than §4 asks. No ledger entry holds this.** cadence §4 says never edit the review comment, and the `/cadence` skill (its parity half, §7) says to post a new one if there is more to say; samscrape's grinders do exactly that: an independent `cadence-review: SHIP · Sonnet · …` first, then their own `cadence-review: FIXED · Opus 5 (author) · … findings addressed` disclosing what they fixed before merging. `check_cadence.py` surfaces the later one, so the row's reviewer field reads `Opus 5 (author)`. Seen on samscrape #830, #833, #838 and #839 — three of the six most recently merged worker PRs, four of the last seven — each verified by reading the comments: every one had a genuine independent Sonnet review underneath. **Why it is more than cosmetic: I nearly filed a board line accusing tdgrind-2 of skipping independent review on #830, on the strength of that row, and stopped only because I read the comments first.** The weekly `/stranded-work` sweep reads these rows in bulk over a window (cadence §4, `--since 7d`) and has no such habit, so the next sweep over this window will see a run of apparent self-reviews on a repo whose reviews were in fact independent — a false accusation is the likely output, and the disclosure convention is what triggers it, which punishes the careful worker. Cheap fixes, your call: report the **first** matching comment (the review is by definition the earliest), or report every match and let the reader see both, or have the row name the reviewer from the earliest match while keeping the latest verdict. The row's real purpose — evidence existed, unedited, before the merge — is satisfied in every one of these cases either way, so nothing is failing today. Needs your call on whether this becomes a dev-cadence TD (the script is SYNC; an orchestrator never files its own). Context: `scripts/check_cadence.py` `REVIEW_RE` and its comment selection, cadence.md §4 and §7 (parity pair), samscrape PRs #830/#833/#838/#839. Due: 2026-09-14.
- [x] 2026-09-12 (session a13565ec `tdgrind-ao-1` run 6 on kmaster) — **Folded 2026-09-23 into the one browser walk below.** As written: **TD-028 step 4 merged, one live browser check pending:** restart the agent and the UI, then look at a worker's card (a `report:` line like `TD-034 → #86 · 1/1 done`, dashed when the agent derived it, plus `N filed`) and its Focus page (a **Reports** panel with **Drop** on a claimed item, and a **grants** chip in the header that grants/revokes `orchestrate` on click). The panel and chip are rendered in JavaScript, which this repo has no harness for — the server halves are tested. Context: TD-028 step 4, PR #88. Due: 2026-09-14.
- [x] 2026-09-12 (session a13565ec `tdgrind-ao-1` run 6 on kmaster) — **Decided by Paul 2026-09-23: `decide` is gated on `control` like `send` — TD-116.** As written: **Decide whether `decide` (one session answering another session's permission prompt) needs the `orchestrate` grant.** Left ungated, as design §4.8 lists it, because gating it changes that list and the UI is unaffected either way (it answers as a person, with no caller) — but a worker auto-approving another worker's tool call is the same class of act as `send`. Context: TD-028 step (1) note, design §4.8, §9 invariant 11. Due: 2026-09-19.
- [x] 2026-09-06 (session 6d1b7b88 on kmaster) — **Decided by Paul 2026-09-23: Deny with a reason, yes — TD-117; "allow for this session", no, not now.** As written: Decide design §10's two permission-dialog questions: Deny with a reason (cheap, the hook already carries one) and "allow for this session" (a third smaller button, never the default). Context: TD-008, design §10. Due: 2026-09-20.
- [x] 2026-09-06 (session 6d1b7b88 on kmaster) — **Overtaken 2026-09-23: the UI has been used on kmaster daily since phase 1; what the flows get wrong is the ledger's TD-069/071/095 series.** As written: Use phase 1 for real: reach the UI over `ssh -L 8765:127.0.0.1:8765 kmaster`, WireGuard, or a Cloudflare tunnel (design §4.5), run the agent + UI, point a session at samscrape, and note what the flows get wrong. Context: README "Run it", design §7 phase 1 success test. Due: 2026-09-13.
- [x] 2026-09-13 (session fc71da0e `orc-membership-design` on kmaster) — **Superseded 2026-09-13 (anchor session): Paul authorised stopping every agent and the whole system; the seven workers were killed that evening and the fleet restarts as a team under `ao team start` (design §4.9, TD-040), which sets `controllers` at create — the hand migration below is not needed.** Upgrade the installed agent and attach orchestrator-ao-1 to its five workers, in one sitting — until you do, it holds the `orchestrate` grant and can act on nothing.** TD-036 steps 1, 2, 3 and 5 are merged (PRs #106, #107, #108, #110): membership is live in the code — an acting RPC now needs the grant **and** the caller's id in the target's `controllers`, and an empty list means nobody may act. Every session running right now predates that and has no list, and a person's `ao new` adds no controller (there is no caller to add), so the orchestrator's reach is currently zero the moment the new agent is running. The full procedure, the fleet as read today, the commands and the end-to-end check are in [`docs/briefs/archive/td036-migration.md`](briefs/archive/td036-migration.md) — in short: reinstall `~/.local/share/agentorc-venv` from main at or past `0ff5ea2`, restart `agentorc-agent` (tmux sessions survive it), then `ao control orchestrator-ao-1 add …` for `tdgrind-ao-1` and the four samscrape workers, then `ao status -v` to see `under:` and `members:` agree. **I could not run it myself:** the installed `ao` has no `control` subcommand yet, and upgrading the venv and restarting the agent under five live unattended workers is outside this session's brief. **Two other things want your call:** step (4) (`controllers:` defaults in `.agentorc.yml`) is **blocked** — there is no `.agentorc.yml` loader in the codebase at all, that whole layer is TD-028 step (5), still open, so I asked whether to build a minimal `controllers:`-only reader inside TD-036 (my preference), defer step 4 behind TD-028, or pull TD-028 step 5 forward; and design §10 has a new question from the `guardians` facts — its sessions run in a VS Code devcontainer at `/workspaces/guardians` while `ao` launches tmux on the host, so a guardians orchestrator is either one of ours or a second host, and that decides how phase 2's transport is aimed. **One thing the review turned up that you should know before attaching anything:** §9 invariant 5 — interactive sessions never nudged — is convention, not a gate. `_gate` never looks at the target's `kind`, so an interactive session *can* be put in a `controllers` list and acted on; the invariant as written covers policies (§6), which are not built yet. Nothing has ever been in that position, and the briefs say not to, but your own anchor session is the obvious casualty if one ever is. Filed as **TD-041** with the fix (refuse in the gate and in `set_controllers`, not in a brief). Context: TD-036, TD-041, TD-028 step 5, PRs #106/#107/#108/#110, `docs/briefs/archive/td036-migration.md`, design §4.8 and §10. Due: 2026-09-14.
- [ ] 2026-09-22 (session 027b217f on kmaster) — **After the next promote, check an unattended session refuses a Claude Code peer message.** From an interactive session, `SendMessage` to a worker started after the promote: the worker should show no *Held message from another session* panel, and the sender should be told the message was refused (`crossSessionInbound: refuse` in its `+unattended` layer — the value is from the design, not yet seen working). A session still showing the panel should read `needs-you`, not `idle`, once 20 min pass without a hook (`ao explain <id>` names `held-peer-message`). Context: TD-064 / PR #431. Due: 2026-10-06.
- [x] 2026-09-23 (session `manager-ao-1` on kmaster) — **Done 2026-09-23 (the anchor): #492 and #490 read and merged (#490 rebased past #492's ledger row), promoted as main `b5287c0`; TD-119 decided and TD-046 / TD-071 designed the same afternoon so the next run has work.** Was: **ao-grind ran out of work at 14:48Z and is wound down. Two `src/sessionorc` PRs still wait on your read.** grinder-ao-1: merged #486 (TD-117, Deny with a reason), #488 (TD-008 archived), #491 (TD-119 filed, self-decide a permission prompt); built and reviewed but held for your read (src/sessionorc/): **#490** (TD-116) and **#492** (TD-096, which stays open for your live use after merge). grinder-ao-2: merged #493 and #494 (TD-118 steps 1–2: the ledger header on every open entry, the grinder template filters on it; step 3 is dev-cadence's). Both PRs opened under 30 minutes before wind-down, so neither hit the 2-hour hold threshold — no separate escalation line for them, just this one. Cadence check over the last 12h: every merged PR passes (review recorded, ci, ledger, worktree). Of the 58 open ledger entries, only TD-116/TD-096/TD-118 were pickable; the rest are owned by you, the anchor, or dev-cadence. Context: PR #490 / PR #492 / TD-116 / TD-096 / TD-118. Due: 2026-09-23.

## In-flight (parked by a session)

- (none)
