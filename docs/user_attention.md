# User Attention Board

Items that need the user to act or decide, plus in-flight work parked by a session. Sessions add entries **the moment they arise** and remove them when handled. Keep this file small — durable debt belongs in [technical_debt.md](technical_debt.md); this board is only "a human must act or decide."

All conventions — entry format and the `<host>` semantics, `Due:` dates and snoozing, the `Swept:`/`Swept-deep:` sweep stamps — live in [docs/cadence.md §3](cadence.md#3-never-strand-work). That file is SYNC (improvements reach every consumer on re-sync); this skeleton is SEED (yours, never overwritten), which is why the explanations do not live here (dev-cadence TD-17).

Format: `- [ ] YYYY-MM-DD (session <first-8-of-session-uuid> on <host>, or n/a) — what's needed. Context: TD-NNN / PR #N / branch. Due: YYYY-MM-DD.`

## Needs the user

- [ ] 2026-09-14 (session e2213351 `tdgrind-ao-1` on kmaster) — **`ao wait` exists; the lead briefs do not use it yet, and that is the part that wants your eye.** TD-049 steps (1)–(4) merged (#145): `ao wait --timeout N` blocks until a session whose `controllers` name the caller changes — a `done` with a PR, a state change, a new finding — and returns the changed records, so a lead's tick can end in it instead of a `ScheduleWakeup`. What is left is steps (5) and (6): the fallback interval, the short list of things a lead must do on the timer *regardless* of events (a merged PR from a worker's branch, a new `docs/cadence-changes.md` entry, a worker gone quiet — silence is not an event), and the `loop` instruction in `docs/briefs/orchestrator-ao-1.md`. I stopped short of editing that brief because it changes how the live lead behaves on its next restart. Suggested check after a restart: from the orchestrator's pane, `ao wait --timeout 60`, then mark something `done` on a worker and watch it return in about a second. Context: TD-049, PR #145. Due: 2026-09-19.
- [ ] 2026-09-14 (session e2213351 `tdgrind-ao-1` on kmaster) — **`check_cadence.py` reads the `cadence-review:` verdict only on a comment's *first* line, and nothing says so.** `scripts/check_cadence.py` matches `REVIEW_RE` against `body.splitlines()[:1]`, so a comment that ends with the verdict line — which is how every reviewer is briefed to *write* its report, and how I wrote the evidence for PRs #138 and #139 — reads as **no review at all**, indistinguishable from a skipped one. Both PRs failed on that, not on a missing review. Two things worth your call: (a) whether cadence §4 should say "first line" where it gives the format, since a detector rule nobody can see is one every new session trips once; (b) whether the matcher should scan the whole body, which costs nothing and removes the trap. `scripts/` and `docs/cadence.md` are dev-cadence's, so neither is mine to change. Context: PRs #138, #139, #141; TD-050. Due: 2026-09-18.
- [ ] 2026-09-14 (session e2213351 `tdgrind-ao-1` on kmaster) — **Merged, live check pending on the running agent/UI (restart both):** TD-042 (#139) makes `ao team start` warn when a brief it is about to hand out names a clock time or a run number — worth watching that a real `ao team start ao-grind` prints *nothing*, since every brief in the repo is now clean, and that the Teams strip toasts it if you point a team at a stale brief. TD-028 step 5 (#141) draws the **Grants** checkboxes on New session: open the form, pick the `orchestrator` role and confirm the `orchestrate` box ticks itself with its warning line, then untick it and confirm the started session has no grant (`ao status -v`). TD-047 (#142) makes the Focus composer say which of Send's two jobs it is doing: open Focus on a **working** session and the button should read **→ Steer** with "steers the turn in flight"; on an idle one **→ Send** with "starts a new turn"; on a killed one the composer should be **disabled** with a line pointing at the banner below it. TD-026 (#147) makes that **stops** badge on Focus the control that edits it: click it for a time (`06:00`, `+8h`, an ISO time), empty to clear, and an unattended session with none shows a dim *no stop time* — the thing a person most needs to know and could not see before. One thing worth pressing deliberately, to **confirm a fix rather than find a bug**: press OK without changing anything while a session is inside its wrap-up grace. Before #147 that asked the session to wrap up a second time and deferred the forced kill; it is fixed there (only a *different* time counts as a new run) and covered by a test, so the expected result is that nothing happens. None of the four was checked in a browser — no session in this run has one, which is also why TD-029's last step is still open. Context: TD-042, TD-028, TD-026, TD-047 (archive), PRs #139, #141, #142, #147. Due: 2026-09-17.
- [ ] 2026-09-14 (session 0675b93e `orchestrator-ao-1` run 3 on kmaster) — **A `/compact` leaves a healthy session reading `stalled?` forever, and §6's stall policy would wrap it up for it. No ledger entry holds this.** Observed live on `ao-agentorc-tdgrind-ao-1` at 14:58Z: state `stalled?` (confidence `scraped`), `ao explain` giving no rule and no evidence, `last_hook` 14:28:28Z — while the pane showed a **completed** `/compact` ("Compacted (ctrl+o to see full summary)", five files listed below it) above an **empty composer**. That session is `idle` in substance: every lane item done or dropped, tree clean, nothing running. **The mechanism, from the code:** `translate()` (`src/agentorc/adapters/claude_code/hook.py:43-87`) passes `SessionStart` straight into `STATE_EVENTS` → `working` (`hook.py:24-28`) with **no look at the payload's `source`** — and Claude Code's documented compaction sequence ends by firing `SessionStart` again with `source: "compact"` (`PreCompact` → `PostCompact` → `SessionStart(compact)`; the hooks guide's own "re-inject context after compaction" recipe matches on exactly that). Neither `PreCompact` nor `PostCompact` is in `HOOK_EVENTS` (`adapters/claude_code/__init__.py:30-42`), so nothing ever reports the compaction finishing; the record sits `working` until the liveness cross-check at `agent.py:556-559` flips it to `stalled?` at `STALL_AFTER`. I did not capture the 14:28:28Z payload, so *which* event set `working` is inference — but it is the only registered event the documented sequence fires, and `/compact` is a built-in that most likely never reaches `UserPromptSubmit` at all. **The shape of the bug is already solved next door in the same function:** `SessionEnd` is checked against `SESSION_END_STILL_RUNNING` (`{"clear", "resume"}`, `hook.py:31`) precisely because some lifecycle events are not what they appear — `SessionStart`'s `source` wants the same treatment, or `PostCompact` wants registering as the explicit "compaction over" signal, or both. **Why it is worth your time rather than a curiosity:** auto-compaction is routine for exactly the long-running unattended workers this system exists to watch, so every one of them will eventually read `stalled?` while perfectly healthy. Today that only misleads a human reading the Herd and an orchestrator following its brief (I did not nudge — the tail shows output and the session holds no open reference, so the prescribed nudge had nothing to say). But §6's stall policy is **"`working` with no output past `stall_after` → flag `stalled?`, nudge once, then wrap up"**, and when that becomes agent code it will nudge and then wrap up a session whose only sin was compacting its context. **Sharpened 15:32Z, and it is worse than first written: the mislabel does not clear itself.** At 15:32Z the record still read `stalled?` with `last_hook` unmoved at 14:28:28Z, even though `last_output` had advanced to 15:26:06Z on an otherwise unchanged screen. The reason is that the liveness check is a one-way door: `agent.py:559` is the only assignment of `stalled?` anywhere, and its branch is guarded on `s.state == "working"`, so once the record leaves `working` that branch is structurally unreachable and no amount of fresh `last_output` can bring it back. Two paths *can* clear the state, and neither fires here: any hook carrying a mapped state reaches `_apply_event` (`agent.py:574-594`), whose `set_state` is unconditional — `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, `SessionEnd` and `Notification` would all clear it — and the screen fallback (`_screen_verdict`, `agent.py:552-555,566-571`) would clear it if a rule matched, since it is not guarded on the current state. But a session resting at an empty composer emits no hook at all, and `ao explain` answers `match: null, reason: "no screen rule matched"` because no rule covers an idle composer. So the mislabel is not transient: nothing the system does on its own corrects it, and in practice it clears only when somebody submits a prompt — a submitted prompt being the only one of those events an idle session is likely to produce. That is precisely the window a supervisor acts in, which is what turns a cosmetic wrong badge into a policy that would nudge and then wrap up a healthy worker. Needs your call on whether this becomes a TD (an orchestrator never files its own) and on the fix shape. Not acted on: the sub-agent that confirmed the hook semantics suggested adding the handler to this repo's `.claude/settings.json` — that is both outside my brief and the wrong layer, since agentorc's hooks reach a session as a generated `--settings` layer (design §4.2), never through a repo's own file. Context: `src/agentorc/adapters/claude_code/hook.py:24-31,43-87`, `adapters/claude_code/__init__.py:30-42`, `src/sessionorc/agent.py:548-559`, design §4.2 (hook → state table) and §6 (stall policy), Claude Code hooks reference (`PreCompact`/`PostCompact`, `SessionStart` matcher `compact`). Due: 2026-09-15.
- [ ] 2026-09-13 (session e2213351 `tdgrind-ao-1` on kmaster) — **Merged, live check pending on the running agent/UI (restart both), and two of these want your eyes rather than a restart.** (1) **A stop time for unattended sessions** (TD-026, #129 + #131): `ao new --unattended --until 06:00|+8h`, `ao until <session> <when>|--clear`, *stops 06:00* on the card, the Focus header and `ao status -v`, and an **Until** field on New session. At the time the agent sends the wrap-up prompt once and stops the session when it settles or ten minutes later — so the thing to watch is a worker that *does not* stop — the wrap-up never landing, or the ten minutes passing without a kill — rather than one stopping early, which the code guards structurally. This is the gap that left hand-started workers running with nobody to close them. (2) **A stood-down worker reads `stalled?`** (TD-032 a/c, #125), from a screen rule written off the quoted banner in TD-032 rather than a captured pane — **if you ever see a real Remote Control stand-down, save the pane**: the rule needs both the close code and a `/rc failed` footer, and only a real screen can say whether that pair is right. (3) **The Focus terminal now uses VS Code's Dark Modern palette** (TD-038 b, #132) — open a session in Focus beside the same one in VS Code's terminal and see whether the colours match; the font and the WebGL renderer are still open, and (c) needs the vendored xterm.js's version identified first. (4) **The mockups are current again** (TD-037, #127), but nothing rendered them: `docs/mockups/shot.sh` needs a Chromium and none of the sessions in this run had one — worth one run and a re-seed of the canvas. Context: TD-026, TD-032, TD-037, TD-038; PRs #125, #127, #129, #131, #132. Due: 2026-09-17.
- [ ] 2026-09-14 (session 0675b93e `orchestrator-ao-1` run 3 on kmaster) — **The cadence check's `review` row, once failed on a merged PR, can never be cleared — the only honest response leaves a permanently red row, and my brief's "second fail on the same PR" escalation fires on it whether or not the worker did anything wrong. No ledger entry holds this.** Observed on agentorc **PR #128** (`ledger-board-tdgrind`, a one-line board append, merged 01:42:48Z): the row read `no cadence-review: comment on the PR`, I sent `tdgrind-ao-1` the single nudge the brief prescribes, and it did the best thing available — at 01:42:58Z it posted a note recording that the review had *not* happened before the merge rather than back-filling a claim that it had, then at 01:53:33Z posted the evidence comment itself, labelled `(post-merge)`, verified the board line's every claim against main, and stated that a board-line PR is not exempt and every future PR gets its comment before the merge. The re-check still fails, now with `review comment posted after the merge (2026-09-14T01:53:33+00:00 > merge)`, and it will fail forever: `row_review` compares the comment's `created_at` to the merge time, and GitHub stamps both, so no action by anyone can make this PR's row green. That is the intended design — §4's whole point is that a timestamp cannot be rewritten — but it has a consequence nobody has written down: **a missed review is an unclearable state, not a fixable one**, so the check's per-PR verdict and every window sweep (`--since 7d`, `/stranded-work`) will carry this PR as a fail indefinitely. The same shape as the immortal `derived` claim this orchestrator filed on 2026-09-12, arriving from the other direction — there, a claim that could never be retired; here, a fail that can never be cleared. That one has since been **fixed and closed** (TD-045, PR #126, merged by `tdgrind-ao-1` earlier in this same run: a claim that resolves to `done` at the merge is no longer immortal), so it is offered as the precedent for how this class of problem gets settled, not as a live sibling. **Why it needs you rather than a worker:** the worker's behaviour was correct and a second nudge would punish honesty — a worker that learns the row is the thing being graded will back-date or edit a comment to clear it, which is precisely the failure §4 exists to prevent. So the question is about the *detector*, your call: should a fail whose cause is in the past be expressible as **acknowledged** (a distinct status the sweep can skip, e.g. a follow-up comment the row reads), or should the window sweep report unclearable fails separately from actionable ones, or is a permanently red row the correct and intended cost of a missed review? Related: it is also the brief's escalation rule that needs the answer — "a second fail on the same PR escalates" cannot distinguish a worker ignoring a nudge from a worker that answered it perfectly, which is the only reason this line exists. Not nudged twice; the worker's own commitment ("every PR from this session gets its evidence comment before the merge from here on") is the forward fix and needs nothing from you. Context: PR #128 and its two comments, `scripts/check_cadence.py` `row_review`, cadence.md §4 and §7, docs/briefs/orchestrator-ao-1.md (escalation rule), design §4.8. Due: 2026-09-14.
- [ ] 2026-09-13 (session e2213351 `tdgrind-ao-1` on kmaster) — **Merged, live check pending on the running agent/UI (restart both):** TD-043 (#122) gives every `ao send` its own tmux paste buffer — the fix for the orchestrator's `no buffer ao-paste`, so it can stop serialising its sends; TD-045 (#126) retires a derived claim whose branch never grew a PR, so a worker that abandoned a branch stops reading as having open work (watch that no *live* claim disappears — the retirement asks `gh` per branch and keeps everything on an outage). TD-044 (#123) is test-only. The one-line thing to watch: after the restart, does any card's `report:` line lose an entry it should have kept? Context: TD-043, TD-044, TD-045 (archive), PRs #122, #123, #126. Due: 2026-09-16.
- [ ] 2026-09-13 (session b4f95a9e, the anchor, on kmaster) — **samscrape has no `.agentorc.yml` and no `docs/briefs/`, so its team is defined but deliberately not started.** `~/.agentorc/org.yml` now defines two teams (design §4.9): `ao-grind` is **running** (orchestrator-ao-1 over tdgrind-ao-1, both on the repo's own briefs), and `samscrape-grind` (an orchestrator over three grinders) is defined and stopped. Starting it today would run its grinders on the package's generic grinder template and lose the standing rules that lived in `~/.tdgrind/tdgrind-*-prompt.md`: the avoid-list, the CRLF files, no cluster deploys, the coordination rule between siblings. Those are samscrape's judgement, not an agentorc session's to write across repos, so the ask is: have a samscrape session add `.agentorc.yml` (at least `roles:` with a grinder brief, `ledger:`, `controllers:`) and `docs/briefs/` carrying those rules, then `ao team start samscrape-grind` here. Until then samscrape has no workers at all — the four that ran until tonight were killed as part of the rebuild. Context: design §4.9, TD-040, `~/.agentorc/org.yml`. Due: 2026-09-17.
- [ ] 2026-09-10 (session 9b6d2ce8 `tdgrind-ao-1` run 3 on kmaster) — **TD-027 merged, live check pending — and the four "stuck composers" need no Enter:** the three samscrape composers show Claude Code's *faint* suggested-next-prompt text (read-only `capture-pane -e`), not an unsubmitted prompt — nothing ever sent them a wrap-up (they run under agentorc with no supervisor: TD-026 gap 1), so close them however you close a finished worker; `ao-agentorc-agentorc-tests-2` is a dead pane that exited on `/exit` on 2026-09-09. After restarting the agent, one `ao send --wait <id> …` to a Claude session should return normally (`prompt-stuck` if the composer keeps the text); `ao send` is ~0.2 s slower per call (it waits for the paste to paint). Context: TD-027 (archive), PR #60. Due: 2026-09-13.
- [ ] 2026-09-10 (session 31a6d882 `tdgrind-ao-1` on kmaster) — **Merged overnight, live check pending on the running agent/UI (restart both to pick them up):** `limited` from the usage endpoint + the top-bar usage chip (TD-001, #44); "finished · unseen" idle cards (TD-017, #43); `ao send --wait` (TD-016, #42); screen rules — a fresh session's trust dialog should show `needs-you (scraped)` within a tick, `ao explain <id>` shows why (TD-015, #47); `ao new --attach` / `ao focus <id>` (TD-010 b, #46); `ao --json` everywhere (TD-018, #41); TD-009/011/012/013/020/021 fixes and TD-007 accepted. The one-line thing to watch: does anything in the Team look wrong after the restart? Context: docs/technical_debt_archive.md entries dated 2026-09-10. Due: 2026-09-13.
- [ ] 2026-09-10 (session 38caa51c `tdgrind-ao-1` run 2 on kmaster) — **Merged, live check pending:** run 2 merged TD-024 (#51), TD-023 (#52: killed sessions offer Details, `ao focus` refuses cleanly), TD-004 minus ssh (#55: `hosts.yml` `local` entry takes `volatile`, `repos_registry`, `runs_keep_days`; the `AGENTORC_*_HOST` env vars are gone — if the agentorc-ui unit sets any, move them into the file), and TD-010 (a) (read-only *registry* cards for your VS Code `claude` sessions — the phase 1 success test's last gap: after restarting the agent every live session on kmaster should show with the right state within a tick; ledger anything wrong). `agentorc-agent serve` now stops by cancelling the serve task (TD-024) — after the next `systemctl --user restart agentorc-agent`, `journalctl --user -u agentorc-agent` should show no `Task was destroyed` ERROR line and the old socket file gone. Context: TD-024 (archive). Due: 2026-09-13.
- [ ] 2026-09-11 (session 070fdec7 `tdgrind-ao-1` run 5 on kmaster) — **Run 5 merged four items; restart the agent and the UI to pick them up, then two things to look at.** (1) `ao progress` / `ao finding` exist now (TD-028 step 2) but the *running* agent does not know the RPCs — this session could not declare its own progress, it got `unknown method 'progress'`; after the restart, `ao status -v` should show a `report:` line for any session that declares one, and the tick should fill in `derived` entries from branches and merged PRs within five minutes (step 3). (2) Every card should now show the model actually in use as the third part of its profile line (TD-031) — `claude-code · paul · opus-5`, or `… (profile)` until the first observation; a `/model` switch should change it within a tick. Also live: one name, one session (TD-030) — starting a name twice refuses with **Switch to**, an exited one is replaced with its run log kept, and `ao focus <name>` takes a bare name. Context: TD-028, TD-030, TD-031 (archived), PRs #69, #70, #72, #75, #77. Due: 2026-09-14.
- [ ] 2026-09-11 (session 070fdec7 `tdgrind-ao-1` run 5 on kmaster) — **TD-029 merged, one live browser check pending:** open Focus on a session and press **Close**. Expect one `[agentorc] this session's pane is gone` line and **no** further `/term/…` attempts in the UI journal (before the fix it reconnected twice a second until Forget). The server half is covered by tests; (a) and (c) are JavaScript, which this repo has no harness for — so this one needs your browser. Restart `agentorc-ui` (and the agent) first to pick the change up. Context: TD-029, PR #71. Due: 2026-09-14.
- [ ] 2026-09-12 (session b9d7bd29 `orchestrator-ao-1` run 2 on kmaster) — **`ao send --wait` reported `prompt-stalled` for a prompt that in fact ran; no ledger entry holds this.** At 2026-09-12 ~13:44Z the first of four relay sends to `ao-samscrape-td377-enumeration` returned `prompt-stalled: ao-samscrape-td377-enumeration showed no activity within 5 s` and exit 1. It had not stalled: `ao explain` immediately afterwards showed the turn **done at 13:45:25Z** (`last_hook` 13:45:27Z, state `idle` by hook) with a 27 s answer to that exact relay text on screen. The window is `SEND_STALL_SECONDS = 5.0` (`src/sessionorc/agent.py:55`), and `rpc_send`'s wait leg treats *any* record revision change as "started" (`agent.py:873-877`) — so the 5 s is the budget for the first hook of the new turn to reach the agent, not for the tool to begin thinking. The session was a long one (its pane footer read 372.1k tokens), which is the obvious suspect for a first hook later than 5 s, but the mechanism is not measured. Relays 2-4 to the same session minutes later returned `idle` normally, so it is intermittent, not a broken path. **Reproduced 14:55:45Z on a second session, and the long-context theory does not survive it:** the nudge to `ao-agentorc-tdgrind-ao-1` (up about an hour, nothing like 372k tokens) returned the same `prompt-stalled … within 5 s`, and the prompt had run — the record gained `TD-025 done #91 source: declared`, which only that prompt could have caused, with the turn settling at 14:55:50Z. So this occurrence is proved by its *effect*, not by a screen reading, and both a fresh short session and a 372k-token one hit it. Two in eight sends. Why it matters more than a cosmetic error: `prompt-stalled` is exactly the signal `ao --skill` and the orchestrator brief tell a caller to act on, and the sanctioned action after one is to look and then decide again — a caller that instead retried would paste a second prompt into a live turn, which is the TD-027 concatenation failure arriving through the front door. Handled here by verifying every send against `ao explain`/`ao tail` rather than its return value. Needs your call on whether this becomes a TD (an orchestrator never files its own), and on the shape of a fix — a longer or adapter-supplied window, or `prompt-stalled` re-checked against the composer being empty before it is raised. Context: `src/sessionorc/agent.py:55`, `agent.py:855-880`, TD-016 (archive), TD-027 (archive), design §4.2. Due: 2026-09-13.
- [ ] 2026-09-12 (session b9d7bd29 `orchestrator-ao-1` run 2 on kmaster) — **The cadence check's `deploy` row answers from whichever clone it is run in, so a behind checkout turns a `fail` into an `na` — it silently suppressed a real finding on five samscrape PRs tonight; no ledger entry holds this (dev-cadence's, samscrape's, or agentorc's).** `row_deploy(pr, root)` (`scripts/check_cadence.py:364-370`, a dev-cadence SYNC file) tests `<root>/scripts/cadence_deploy_check.sh` in the *working tree* it is invoked from. samscrape PR #796 added that script (mode 100755, with a test) at 14:21:47Z, but `/home/kmaster/samscrape` is 6 commits behind `origin/main`, so from there the row reads `na — no executable scripts/cadence_deploy_check.sh` for every PR. Measured from a detached worktree at `origin/main` instead, the same PRs read **fail**: #796 `not deployed: dev1-samscrape-company-api-… has no /app/scripts/cadence_deploy_check.sh`, and #791/#792/#793/#794/#795 each `not deployed: /app content predates <sha> … — skaffold run`. So the row works and its verdict is correct; what is wrong is that its input is the invoker's checkout, and an orchestrator or a `/stranded-work` sweep runs it out of whatever clone it happens to sit in. A detector that reports `na` from a stale clone and `fail` from a fresh one is worse than one that always fails, because `na` reads as "this repo has no deploy gate" — which is what I logged at ticks 3-5 before measuring. Fix shape already exists in the same toolkit: dev-cadence TD-030's reader half has `nudge_user_attention.py --fetch` read a merely-behind clone's board from `origin/<default>` rather than the local file; the `deploy` row wants the same treatment (or an explicit `unknown` when the tree is behind its upstream, which is honest and cheap). The fix belongs in **eyecantell/dev-cadence**, not here and not in samscrape, because the script is SYNC. Not nudged to any worker: the five fails all say `skaffold run`, which is your action and is already asked for by two samscrape board items due today (its 2026-09-10 and 2026-09-11 entries, both `Due: 2026-09-12`), with several older lines overdue behind the same un-run rollout — the workers cannot clear it and deploying is outside every one of their briefs. The useful half of the news is that #796 makes those existing board items machine-checkable for the first time. Needs your call on whether this becomes a dev-cadence TD (an orchestrator never files its own). Context: `scripts/check_cadence.py:364-390`, samscrape PR #796, cadence.md §5 and §3, dev-cadence TD-030. Due: 2026-09-13.
- [ ] 2026-09-12 (session b9d7bd29 `orchestrator-ao-1` run 2 on kmaster) — **The cadence check's `review` row names the *last* `cadence-review:` comment, so a PR whose author discloses its post-review fixes is reported as having reviewed itself — it reads as a §4 violation by a worker that did more than §4 asks. No ledger entry holds this.** cadence §4 says never edit the review comment, and the `/cadence` skill (its parity half, §7) says to post a new one if there is more to say; samscrape's grinders do exactly that: an independent `cadence-review: SHIP · Sonnet · …` first, then their own `cadence-review: FIXED · Opus 5 (author) · … findings addressed` disclosing what they fixed before merging. `check_cadence.py` surfaces the later one, so the row's reviewer field reads `Opus 5 (author)`. Seen on samscrape #830, #833, #838 and #839 — three of the six most recently merged worker PRs, four of the last seven — each verified by reading the comments: every one had a genuine independent Sonnet review underneath. **Why it is more than cosmetic: I nearly filed a board line accusing tdgrind-2 of skipping independent review on #830, on the strength of that row, and stopped only because I read the comments first.** The weekly `/stranded-work` sweep reads these rows in bulk over a window (cadence §4, `--since 7d`) and has no such habit, so the next sweep over this window will see a run of apparent self-reviews on a repo whose reviews were in fact independent — a false accusation is the likely output, and the disclosure convention is what triggers it, which punishes the careful worker. Cheap fixes, your call: report the **first** matching comment (the review is by definition the earliest), or report every match and let the reader see both, or have the row name the reviewer from the earliest match while keeping the latest verdict. The row's real purpose — evidence existed, unedited, before the merge — is satisfied in every one of these cases either way, so nothing is failing today. Needs your call on whether this becomes a dev-cadence TD (the script is SYNC; an orchestrator never files its own). Context: `scripts/check_cadence.py` `REVIEW_RE` and its comment selection, cadence.md §4 and §7 (parity pair), samscrape PRs #830/#833/#838/#839. Due: 2026-09-14.
- [ ] 2026-09-12 (session a13565ec `tdgrind-ao-1` run 6 on kmaster) — **TD-028 step 4 merged, one live browser check pending:** restart the agent and the UI, then look at a worker's card (a `report:` line like `TD-034 → #86 · 1/1 done`, dashed when the agent derived it, plus `N filed`) and its Focus page (a **Reports** panel with **Drop** on a claimed item, and a **grants** chip in the header that grants/revokes `orchestrate` on click). The panel and chip are rendered in JavaScript, which this repo has no harness for — the server halves are tested. Context: TD-028 step 4, PR #88. Due: 2026-09-14.
- [ ] 2026-09-12 (session a13565ec `tdgrind-ao-1` run 6 on kmaster) — **Decide whether `decide` (one session answering another session's permission prompt) needs the `orchestrate` grant.** Left ungated, as design §4.8 lists it, because gating it changes that list and the UI is unaffected either way (it answers as a person, with no caller) — but a worker auto-approving another worker's tool call is the same class of act as `send`. Context: TD-028 step (1) note, design §4.8, §9 invariant 11. Due: 2026-09-19.
- [ ] 2026-09-06 (session 6d1b7b88 on kmaster) — Decide design §10's two permission-dialog questions: Deny with a reason (cheap, the hook already carries one) and "allow for this session" (a third smaller button, never the default). Context: TD-008, design §10. Due: 2026-09-20.
- [ ] 2026-09-06 (session 6d1b7b88 on kmaster) — Use phase 1 for real: reach the UI over `ssh -L 8765:127.0.0.1:8765 kmaster`, WireGuard, or a Cloudflare tunnel (design §4.5), run the agent + UI, point a session at samscrape, and note what the flows get wrong. Context: README "Run it", design §7 phase 1 success test. Due: 2026-09-13.
- [x] 2026-09-13 (session fc71da0e `orc-membership-design` on kmaster) — **Superseded 2026-09-13 (anchor session): Paul authorised stopping every agent and the whole system; the seven workers were killed that evening and the fleet restarts as a team under `ao team start` (design §4.9, TD-040), which sets `controllers` at create — the hand migration below is not needed.** Upgrade the installed agent and attach orchestrator-ao-1 to its five workers, in one sitting — until you do, it holds the `orchestrate` grant and can act on nothing.** TD-036 steps 1, 2, 3 and 5 are merged (PRs #106, #107, #108, #110): membership is live in the code — an acting RPC now needs the grant **and** the caller's id in the target's `controllers`, and an empty list means nobody may act. Every session running right now predates that and has no list, and a person's `ao new` adds no controller (there is no caller to add), so the orchestrator's reach is currently zero the moment the new agent is running. The full procedure, the fleet as read today, the commands and the end-to-end check are in [`docs/briefs/td036-migration.md`](briefs/td036-migration.md) — in short: reinstall `~/.local/share/agentorc-venv` from main at or past `0ff5ea2`, restart `agentorc-agent` (tmux sessions survive it), then `ao control orchestrator-ao-1 add …` for `tdgrind-ao-1` and the four samscrape workers, then `ao status -v` to see `under:` and `members:` agree. **I could not run it myself:** the installed `ao` has no `control` subcommand yet, and upgrading the venv and restarting the agent under five live unattended workers is outside this session's brief. **Two other things want your call:** step (4) (`controllers:` defaults in `.agentorc.yml`) is **blocked** — there is no `.agentorc.yml` loader in the codebase at all, that whole layer is TD-028 step (5), still open, so I asked whether to build a minimal `controllers:`-only reader inside TD-036 (my preference), defer step 4 behind TD-028, or pull TD-028 step 5 forward; and design §10 has a new question from the `guardians` facts — its sessions run in a VS Code devcontainer at `/workspaces/guardians` while `ao` launches tmux on the host, so a guardians orchestrator is either one of ours or a second host, and that decides how phase 2's transport is aimed. **One thing the review turned up that you should know before attaching anything:** §9 invariant 5 — interactive sessions never nudged — is convention, not a gate. `_gate` never looks at the target's `kind`, so an interactive session *can* be put in a `controllers` list and acted on; the invariant as written covers policies (§6), which are not built yet. Nothing has ever been in that position, and the briefs say not to, but your own anchor session is the obvious casualty if one ever is. Filed as **TD-041** with the fix (refuse in the gate and in `set_controllers`, not in a brief). Context: TD-036, TD-041, TD-028 step 5, PRs #106/#107/#108/#110, `docs/briefs/td036-migration.md`, design §4.8 and §10. Due: 2026-09-14.


## In-flight (parked by a session)

- (none)
